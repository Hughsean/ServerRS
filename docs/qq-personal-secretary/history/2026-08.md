# 个人 QQ 智能秘书开发历史：2026-08 归档

> 最后更新：2026-08-02
> 本文件只记录已经发生且有证据的事项；未来计划统一放在 `TODO.md`。

## 2026-08-02 20:28（Asia/Shanghai）：CTX-004-VERIFY MySQL 主路径真实通过

- **MySQL Replan 闭环测试（mysql_action_planner.rs，`#[ignore]`）**：
  - 测试函数：`mysql_replan_two_rounds_effect_and_response_singleton`。
  - 基础设施：`ReplanPlanner`（Round 1: SearchRecentEvents / Round 2: NoAction，从
    `input.command.source_event_id` 提取证据）、真实 MySQL ActionStore（`build_mysql_action_store`）、
    MySQL CheckpointStore（`PlannerUseCase::with_checkpoint_db`）。
  - 运行方式：`PlannerUseCase::run_once` 完整生命周期
    （`ensure_action_run → claim_pending_run → execute_claimed → build_action_graph →
    run_checkpointed → mark_completed`）。
  - 断言：Planner 调用 2 次（`ReplanPlanner.calls == 2`）、
    `secretary_action_effect_receipts` 恰好 1 行、`secretary_action_responses` 恰好 1 行、
    响应文本不含 JSON 字段名（`query_effect`/`version`/`tool_kind`/`typed_events`）、
    run status 为 `completed`。
  - **幂等性**：重建 `build_mysql_action_store` 后 `load_effect_receipt(run_id, effect_id)` 返回持久化回执
    （`result_ref` 可解析为 `QueryEffectResultV1`、`version == 1`、summary 描述查询结果），
    effect_receipts 仍为 1 行（无重复）。
  - 发现的次要事实：MySQL `apply_effect` 和 `load_effect_receipt` 均硬编码 `tool_kind: None`；
    `tool_kind` 仅由 `SecretaryActionEffectExecutor::execute` 在 Store 返回后内存中设置，
    不作为持久化字段存储。当前 `PlannerUseCase` 和 Graph 流程不依赖该字段的持久化值，
    且新执行与缓存重放返回前都会从当前 Proposal 补回，因此不影响 Replan 一致性校验；
    仅作为仓储返回值完整性的后续优化。

- **独立验证**：
  - `cargo fmt --check` 通过；两个 crate 严格 Clippy 通过。
  - personal-secretary 236 条 lib 测试通过；qqbot-server 117 条 lib 测试通过。
  - MySQL 测试在随机隔离 `qqbot_accept_*` schema 中真实运行并通过（清理已执行）。
  - `2026-08-02 20:33（Asia/Shanghai）` Codex 使用健康的 `serverrs-qqbot-mysql` 再次创建一次性
    随机 schema 独立重跑，结果 1/1 通过；finally 路径已删除该 schema，未触碰数字人数据库。

- 未提交、推送或合并，`.mcp.json` 未触碰。

## 2026-08-02 20:05（Asia/Shanghai）：Codex 独立复核 CTX-004-VERIFY Graph 与隐私视图证据

- **全 Graph 集成测试（personal-secretary/src/action_graph/tests.rs）**：
  - `replan_full_graph_two_rounds_search_then_no_action`：
    RecordingPlanner（Round 1: SearchRecentEvents / Round 2: NoAction）+ FakeEffectExecutor
    （返回含 typed_events 的 QueryEffectResultV1）运行生产等价的 4 个不同节点、7 次节点访问。断言：
    Planner 调用 2 次（第二次输入的 replan_round 为 1）、Effect 执行 1 次、Outcome 与 ResponseReady 各 1 份。
    Round 2 的 PlannerInput 包含 1 条观察（tool_kind/typed_events/source_event_ids 均正确）。
  - `replan_full_graph_budget_exhausted_finishes`：
    所有 Planner 调用均返回 SearchRecentEvents Proposal（永不返回 NoAction），
    MAX_REPLAN_ROUNDS=2 用尽后 ReplanRouter 返回 finish，BuildResponseNode 从
    last_receipt 构造响应并设置 Outcome。断言 Planner 恰好调用 2 次、
    Effect Receipt 恰好 2 条、replan_round 达到上限、Outcome 存在。
  - 新增测试基础设施：RecordingPlanner（记录调用/按序返回预设输出）、
    FakeEffectExecutor（动态构造含 QueryEffectResultV1 的 receipt）。

- **隐私视图测试（qqbot-server/src/action_planner.rs）**：
  - `observation_with_typed_events_maps_to_temp_refs_not_real_ids`：
    构造含 typed_events（真实事件 ID "real-search-event-1"、真实 actor ID "alice"）的
    PlannerToolObservation 作为 Replan 第二轮输入。通过 FakeClient 捕获 LLM 输入 JSON，
    断言 `real-search-event-1` 和 `alice` 不出现在序列化文本中；
    `evt_` 和 `actor_` 临时引用正确出现；tool_observations 含 `[不可信工具数据]` 前缀。
  - `observation_without_typed_events_only_shows_count`：
    验证 typed_events 为空时 LLM 输入只包含有界计数（"涉及 1 条来源事件"），
    绝不含原始 summary 中的真实 ID 或文本。

- **独立验证**：
  - personal-secretary 236 条测试（+2 Graph 测试）全部通过。
  - qqbot-server 117 条测试（+2 隐私视图测试）全部通过。
  - 两个 crate 严格 Clippy（`-D warnings`）通过。
  - `cargo fmt --check` 通过。

- **MySQL 主路径**：仓库中尚无 CTX-004 对应测试，不能仅以 `QQBOT_TEST_DATABASE_URL` 未设置解释为
  “跳过”。本次复核确认 `serverrs-qqbot-mysql` 健康运行；需先补一条最小随机隔离 MySQL 主路径再执行。
- **未提交、推送或合并**；`.mcp.json` 未触碰。

## 2026-08-02 19:46（Asia/Shanghai）：Codex 独立复核 CTX-004 第四轮修复

- P0-PRIVACY（fail-open 全部消除）：
  - GetThreadContext 和 ResolveReference 已从 `is_replan_observation_tool()` 移出（摘要含 thread/actor/evidence ID，无法安全类型化投影）。
  - `build_llm_views()` 的 typed_events 为空分支不再回退 raw summary + String::replace，改为只输出有界计数（"查询完成，涉及 N 条来源事件"）。
  - typed_event.source_event_id 无临时映射时 `build_llm_views` 返回 `PlannerError::InvalidOutput`（fail-closed），不再返回真实 ID。
  - 新增校验：typed_event.source_event_id 必须属于 observation.source_event_ids。
- P0-RESPONSE（预算耗尽 JSON 泄露）：
  - BuildResponseNode 的 last_receipt 路径先尝试解析 `QueryEffectResultV1`，成功则用 `summary` 构造 OwnerResponseDraft；解析失败才回退既有 `build_action_response_draft`。
- P1（typed_events 状态校验）：
  - `validate_tool_observation` 新增：typed_events 数量 ≤ `MAX_TYPED_EVENTS_PER_OBSERVATION`(30)、actor_id 非空且 ≤ 256 字节、excerpt ≤ 200 字符、source_event_id 去重、必须属于 observation.source_event_ids。
  - 新增常量：`MAX_TYPED_EVENTS_PER_OBSERVATION`、`MAX_TYPED_EVENT_EXCERPT_CHARS`、`MAX_TYPED_EVENT_ACTOR_ID_BYTES`。
- 独立验证：
  - `cargo fmt --all -- --check` 通过。
  - personal-secretary 与 qqbot-server 严格 Clippy（`-D warnings`）通过。
  - action_graph 聚焦测试 29 条、action_planner 聚焦测试 25 条全部通过。
  - `git diff --check` 通过。
- 仍缺：CTX-004-VERIFY（全 Graph/MySQL 闭环测试）。未提交、推送或合并，`.mcp.json` 未触碰。

## 2026-08-02 19:14（Asia/Shanghai）：CTX-004 第三轮修复部分通过

- 原记录误写尚未到达的 21:30，已按 Codex 实际复核分钟纠正。
- 已正确修复：Search/Read 增加 `QueryEffectTypedEvent`，LLM 投影使用 evt_N/actor_N；已有 Outcome
  时 BuildResponse 直接从 response_text 构造 OwnerResponseDraft，不再丢失最终回答。
- P0 仍在：typed_events 为空时回退 raw summary + String::replace；GetThreadContext/ResolveReference
  的摘要可能含稳定 thread/actor/evidence ID；typed event 无映射时以真实 source_event_id 兜底。
- 预算达到两轮且没有最终 Outcome 时，BuildResponse 仍从结构化 last_receipt 构造响应，可能显示
  QueryEffectResultV1 JSON。typed_events 的数量、actor_id/excerpt 长度、去重和必须属于
  source_event_ids 的一致性尚未校验。
- 全 Graph 与随机隔离 MySQL 主路径仍缺。当前不批准提交；未连接 QQ/NapCat，`.mcp.json` 未触碰。

## 2026-08-02 18:54（Asia/Shanghai）：CTX-004 第二轮修复仍未关闭两个 P0

- Observation 修复仅把 typed source_event_ids 注册为 evt_N，并对 summary 执行字符串 replace。
  QueryEffectResultV1 没有 Actor/Thread/Claim 等类型化字段，因此 Search/Read 的 actor_id、线程上下文
  的 thread_id、actor_id、claimant_actor_id、raised_by_actor_id 仍原样进入远程模型；这不属于完整
  TempRefMap 投影，也违反“不使用字符串替换猜测标识”的任务边界。
- BuildResponseNode 取得 `outcome.response_text()` 后命名为 `_text`，实际仍调用
  `build_action_response_draft(last_receipt, ...)`。第二轮最终回答没有进入 ResponseReady，预算耗尽时
  QueryEffectResultV1 JSON 仍可能成为 Owner 文案。
- 已确认有效的局部修复：Replan 白名单缩减为实际结构化路径的 9 种工具；解析时检查 version=1 与
  tool_kind 一致；validate_agent_state 增加新字段上限。但尚未证明 Checkpoint restore 后执行该校验。
- 没有新增 Fake Planner 全 Graph 或随机隔离 MySQL 闭环测试。当前不批准提交；未连接 QQ/NapCat，
  `.mcp.json` 未触碰。

## 2026-08-02 18:25（Asia/Shanghai）：CTX-004 第一轮独立复核未通过

- 已确认：Graph 使用 `TransitionRule::Branch` 形成 Plan/L0/ReplanDecision 分支；预算固定为两轮，
  旧纯文本回执不会进入 Replan；格式、严格 Clippy、29 项 action_graph 测试和 diff check 通过。
- P0 隐私/可用性：SearchRecentEvents、ReadSourceEvent、GetThreadContext 等摘要包含真实事件、Actor、
  Thread ID，`build_llm_views` 明确“不替换摘要中的真实 ID”并原样发送，`source_event_refs` 固定为空。
  这既泄露稳定标识，也让下一轮无法用 evt_N 继续读取搜索结果。
- P0 响应：第二轮 Planner 设置 Outcome 后 BuildResponse 直接跳过 ResponseReady；PlannerUseCase 完成
  时回退 last_receipt，最终模型回答丢失，结构化 QueryEffectResultV1 JSON 可能成为 Owner 文案。
- P1：声明的 13 种工具中，通知策略和记忆查询仍产生旧格式 result_ref，实际不会 Replan；解析未
  复验 version/tool_kind；新状态字段未纳入 `validate_agent_state` 的反序列化边界校验。
- 当前只有 Node/Router 碎片测试，没有任务要求的 Fake Planner 全 Graph 主路径和真实 MySQL 主路径。
  CTX-004 保持进行中；未提交、推送或合并，未连接 NapCat/QQ 开放平台，`.mcp.json` 未触碰。

## 2026-08-02 15:30（Asia/Shanghai）：CTX-004 有界多轮 Replan 闭环完成

- Graph 拓扑从 `Plan → L0Execute → BuildResponse → End` 改为 `Plan → L0Execute → ReplanDecision → (Plan|BuildResponse) → End`。
- 新增 `PlannerToolObservation`（领域类型）和 `QueryEffectResultV1`（结构化 JSON result_ref）。
- `ReplanDecisionNode`：从 last_receipt 解析 QueryEffectResultV1，追加 `ObservationAppended` 到状态。
- `ReplanRouter`：基于 replan_round ≥ MAX_REPLAN_ROUNDS、tool_kind 非查询、Outcome 已设置等条件决定
  continue（回到 Plan）或 finish（进入 BuildResponse）。
- `SecretaryAgentState` 新增 `replan_round`（`#[serde(default)]`）和 `planning_observations`（`#[serde(default)]`），
  旧 Checkpoint 兼容。
- EffectExecutor 查询工具 now 产生结构化 JSON（`query_effect_json()`），非查询工具保持纯文本。
- LLM 输入新增 `tool_observations`、`replan_round`、`remaining_query_budget` 字段；System Prompt 更新。
- 隐私：观察标记"[不可信工具数据]"，旧纯文本 result_ref 不可解析时不进入 LLM。
- 预算：`MAX_REPLAN_ROUNDS=2`，单条观察 ≤2000 字符，合计 ≤4000 字符；只有 L0ReadOnly 查询工具触发 Replan。
- 验证：234 项 personal-secretary lib 测试通过（+8 项 ReplanDecisionNode/ReplanRouter 新测试），
  25 项 action_planner 测试通过，19 项 workspace_boundaries 测试通过；格式、严格 Clippy 通过。
- 分支 `deepseek/qqbot-bounded-replan-v1`，基于 `c251942`。未连接 NapCat/QQ 开放平台，未发送消息，
  `.mcp.json` 未触碰。未提交、未推送、未合并。

## 2026-08-02 13:57（Asia/Shanghai）：Agent 有界事件证据视图最终复验通过

- 模型引用边界完全收口：`source_event_id`、`thread_id`、`memory_source_event_ids` 与 conversation
  全部只能从本次请求的 TempRefMap 恢复；未知引用返回 InvalidOutput，删除直接构造真实 ID 的旁路。
- MySQL 最近事件查询将 `mentioned_actor_ids` 显式 `CAST(... AS CHAR)`，与 Rust `Option<String>`
  行模型匹配。
- Codex 使用 `serverrs-qqbot-mysql` 当前容器凭据创建随机 `qqbot_accept_codex_*` schema，真实重跑
  `recent_event_views_account_scoped_with_mentions_reply_and_thread`，结果 1/1 通过；schema 在 finally
  删除。第一次复验沿用旧端口/密码导致连接池超时，不属于业务失败，也未成功创建 schema。
- 本切片 `CTX-001/002/003/005` 批准完成，下一切片为 `CTX-004` 有界多轮 Replan。未连接 NapCat、
  QQ 开放平台或发送消息，`.mcp.json` 未触碰。

## 2026-08-02 13:41（Asia/Shanghai）：AgentEventView 第一轮评审修复复核

- 已确认修复：可信 loopback 状态从配置贯穿 Planner UseCase/RunContext/PlanNode/LLM Planner；远程
  模型不再获得 local_only 正文。临时映射现在复用 Actor、Mention、会话和 Thread 标签，Reply
  指向窗口内父事件 evt，命令事件 evt 也进入输入。MySQL 改用有效 Thread 视图，正文投影缺失降级。
- 仍有 P0：`resolve_source_event_id`/`resolve_thread_id` 对非临时前缀直接构造真实 ID，conversation
  允许 raw kind+id 回退，`correct_memory_fact.memory_source_event_ids` 继续使用直接解析函数。模型仍可
  引用当前输入之外的格式合法 ID，尚未实现完整 fail-closed。
- 本地 `serverrs-qqbot-mysql` 实际在线。在随机隔离 schema 重跑唯一 CTX MySQL 测试时，修正后的
  Thread fixture 已成功插入，但 Retriever 查询因 MySQL JSON `mentioned_actor_ids` 直接解码为 Rust
  String 类型不兼容而失败；必须按项目既有方式 `CAST(... AS CHAR)`。schema 已在 finally 清理。
- 当前不得提交或进入 CTX-004；未连接 NapCat/QQ 开放平台，未发送消息，`.mcp.json` 未触碰。

## 2026-08-02 12:07（Asia/Shanghai）：Agent 有界事件证据视图交付复核未通过

- 实现 `CTX-001`：`AgentEventView` 领域类型（`source_event_id`、`conversation`、`actor: ThreadActorRef`、
  `occurred_at_unix_secs`、`role: MessageRole`、`content_trust_level`、`excerpt`、`mentioned_actors`、
  `mention_all`、`reply_to_event_id`、`thread_id`）。校验强制 actor account 匹配、正文 ≤ 1000 字符、
  @ 目标 ≤ 20 个、ID 非空。新增 `AgentEventViewError` 枚举和 `validate_agent_event_view` 纯函数。
  在 `PlannerInput` 新增 `recent_event_views: Vec<AgentEventView>` 字段（保留 `recent_events` 兼容）。

- 实现 `CTX-002`：`PlannerLlmInput` 扩展为 `command` + `recent_event_views`（`RecentEventLlmView` DTO）+
  `retrieved`（`RetrievedLlmView` DTO）+ time 字段。序列化前通过 `TempRefMap` 建立临时引用映射
  （evt_N / actor_N / conv_N / thread_N / mentioned_N_j），真实 ID 绝不进入远程模型。模型输出
  evidence 通过 `resolve_event_refs` 先查 temp ref map、再回退直接 SourceEventId 解析。

- 实现 `CTX-003`：`RetrieverStoreT` 新增 `list_recent_event_views(account, limit)` 方法。MySQL 实现
  四表 JOIN（`secretary_source_events` + `secretary_conversations` + `secretary_message_contents` +
  `secretary_thread_events`），解析 `mentioned_actor_ids` JSON，按 `received_at DESC, source_event_id
  DESC` 倒序取最近 N 条后 Rust 侧反转为正序。`EventViewExcerptMaxChars = 1000`。
  `RetrieverUseCase` 包装，`PlanNode` 调用填充 `recent_event_views`。FakeStore 已补全。

- 系统 prompt 更新：描述新输入格式（`recent_event_views`、`retrieved`、临时引用语义），要求 evidence
  和 `source_event_id` 必须使用 `event_ref`，不得发明或猜测不在输入中的引用。

- 验证：`cargo fmt --check` ✓、`cargo clippy -D warnings` ✓（personal-secretary + qqbot-server）、
  planner 49 ✓、action_planner 25 ✓（含 3 条新 CTX-005 捕获测试：Planner LLM 输入结构验证、临时
  引用解析为真实 ID、回退直接解析）、retriever_service 6 ✓、workspace_boundaries 19 ✓。
  MySQL 集成测试 `recent_event_views_account_scoped_with_mentions_reply_and_thread` 已编写
  （`#[ignore]`，需 QQBOT_TEST_DATABASE_URL），覆盖账号隔离、@ 解析、Reply 引用、Thread 关联、
  limit 限制和排序；未运行。未连接 NapCat/QQ 开放平台，未发送消息。

- Codex 独立复核确认领域视图与 retrieved 序列化方向成立，但当前实现尚不能宣称完成：
  - `LocalOnly` 在 `build_llm_views` 中无条件 `content_visible=true`，Retriever wrapper 没有传入或应用
    已验证 loopback 策略，远程模型会收到 local_only 正文；
  - `TempRefMap` 只保存事件映射，命令事件的 `evt_1` 没有出现在输入；Actor/会话/Thread 每条事件
    重新编号，Mention/Reply 标签无法关联到窗口内实体；Action 字段仍直接解析临时字符串；
  - 未知 event ref 会回退接受任意格式合法 UUID，非法引用被静默丢弃，违反 fail-closed 要求；
  - MySQL 查询使用 `secretary_thread_events` 而不是 `secretary_effective_thread_events`，正文投影缺失
    时 CASE 会回退为 normal。
- 格式、personal-secretary/qqbot-server 严格 Clippy、diff check 通过。随后在随机隔离 schema 中
  真实运行 `recent_event_views_account_scoped_with_mentions_reply_and_thread`，因测试构造的 Thread ID
  超过 `CHAR(36)` 在插入 fixture 时失败，尚未执行 Retriever 断言；schema 已在 finally 清理。
- 原记录使用当时尚未到达的 13:00，现改为实际可验证的 12:07 复核时间。TODO 已恢复为进行中；
  修复并通过真实 MySQL 测试前不得进入 CTX-004。当前未提交、推送或合并，`.mcp.json` 未触碰。

## 2026-08-02 11:06（Asia/Shanghai）：MEM-011 第三轮独立短复验批准

- Codex 逐段复核 deferred 表、远程登记、本地优先领取、连续会话前缀、租约 fencing、提交删除和
  主游标保持语义，确认 `local_only(L1,t1) -> normal(N1,t2)` 的历史可达性反例已经闭合。
- `20260802_personal_secretary_memory_candidates.sql` 在基线 `4e476af` 中不存在，整个文件属于本切片
  首次落地；前两轮只在已删除的随机测试 schema 中执行，因此把 deferred 表纳入该首次迁移不会
  造成已有发布库跳过建表。
- 使用独立 `target/codex-review` 运行 personal-secretary 与 qqbot-server 的严格 Clippy，结果通过；
  启动 `serverrs-qqbot-mysql` 后新建随机隔离 schema，真实运行全部 7 条 `memory_candidate_` 测试，
  7/7 通过，schema 已在 finally 清理。未连接 NapCat/QQ 开放平台，未发送消息。
- 当前切片获准进入单次提交；数字人 CLI 的并行修改与 `.mcp.json` 不属于提交范围，不推送、不合并。

## 2026-08-02 01:15（Asia/Shanghai）：结构化记忆候选第三轮修复 P0-6（local_only 配置切换不可达）

- Codex 第三轮确认第二轮 P0-1（交错会话不丢事件）、P0-2（身份-来源三层绑定）、P1-3（临时引用）、
  P1-4（LEFT JOIN 缺失正文投影失效）和 P1-5（CHECK 按 control_kind 分支）均正确通过；但发现
  新 P0-6 可达性反例：`local_only(L1,t1) → normal(N1,t2)` → 远程模式领取 N1 推进全局游标到 t2 →
  切本地后 L1 因 `received_at > t2` 查询条件永久不可达。
- 修复：在 `20260802_personal_secretary_memory_candidates.sql` 新增
  `secretary_memory_candidate_deferred` 表（`account_id + source_event_id` 主键、事件级联删除、
  按 `received_at` 排序索引），实现持久化逐事件延期状态。
- `mysql_memory_candidate.rs` 新增三个路径：
  1. 远程 `claim`：`INSERT IGNORE` 批次范围内被过滤 local_only（限定全局二元组边界，保证在
     游标之前或等位）→ 远程主领取不重复读到这些事件（`> 游标`）
  2. 本地 `claim`：优先 `claim_deferred_batch` → 清理不可处理死行 → 最早会话连续前缀 →
     `next_cursor = 当前主游标`（不推进，延期事件已在游标之前或等位）
  3. `commit`：同一事务 `DELETE` 本批次延期行（延期批次主清理；本地主批次覆盖游标 NULL 边界
     同事件行；远程主批次空操作）
- 崩溃窗口走查：claim 已提交但 commit 未执行 → 游标未动、INSERT IGNORE 幂等；游标 NULL 时
  跳过延期路径，主领取全量覆盖并清理延期行。
- 新增 Codex 指定回归测试 `memory_candidate_local_only_before_normal_survives_remote_then_local`：
  L1(local_only) + N1(normal) → 远程领 N1 游标越过 L1 → 切本地仍领取 L1 → 排空后无重复。
- 验证（`serverrs-qqbot-mysql` 随机隔离 schema）：
  - `cargo fmt --all -- --check` ✓
  - `cargo clippy -p personal-secretary -p qqbot-server --all-targets -- -D warnings` ✓
  - Unit tests: personal-secretary 223, qqbot-server 112（2 ignored 需真实模型）
  - MySQL 候选切片：**7/7**（原 4 + P0-1 交错 + P0-2 来源 Actor 绑定 + P0-6 配置切换）
  - MySQL 其余：22/30（8 基线失败：gap_freezes、dismiss/snooze/batch ×4、owner_work_control ×2、
    unanswered，非本切片新增）
  - MySQL 其他 binaries：7/7（action_planner）、29/29（acceptance_mysql）、13/13（acceptance_runtime）
- 修正交付报告基线为 `4e476af`（上一份误写 `71a0898`）。
- 临时 schema 已清理；`.mcp.json` 未触碰；未连接 NapCat/QQ 开放平台、未发送消息。
- TODO/HISTORY/月度历史已同步。当前未提交、未推送、未合并；切片代码、迁移与测试已完成，等待
  用户授权提交。

## 2026-08-02 00:04（Asia/Shanghai）：结构化记忆候选第二轮复核仍有游标阻断

- 确认上一轮要求的连续同会话前缀、候选身份与权威来源三层绑定、远程 LLM 批次内临时引用、
  缺失正文投影失效以及 `approve_conflict` 精确版本 CHECK 均已落实。格式检查与两个相关 crate
  严格 Clippy 通过；在 `serverrs-qqbot-mysql` 新建随机隔离 schema 运行全部 6 条
  `memory_candidate_` MySQL 聚焦测试，6/6 通过，schema 已在 finally 清理。
- 复核发现新的历史可达性问题：`secretary_memory_candidate_processing_state` 每账号只有一条游标，
  而远程模型领取时过滤 local_only。若 local_only 事件早于随后可处理的 normal 事件，提交 normal
  批次会把游标推进到前者之后；后续改用已验证回环模型时，较早 local_only 事件永久不可领取。
  现有测试只覆盖 normal 在前、local_only 在后的有利顺序，不能证明配置切换下不丢历史。
- 实际分支 HEAD 为 `4e476af`；交付报告写成基线 `71a0898` 与仓库事实不符，需在最终交付说明中
  更正。当前 `MEM-011` 仍未批准，未提交、推送或合并，未连接 NapCat/QQ 开放平台，也未发送消息。

## 2026-08-01 23:12（Asia/Shanghai）：重构 Claude 长期规则并修正评审方法

- 确认上一轮记忆候选评审主要聚焦功能可达与审批事务，没有用交错会话顺序和 Actor—来源证据
  两条不变量构造反例，因此未提前发现本轮两个 P0；该遗漏属于 Codex 评审不完整，不归因于
  DeepSeek 对上一轮问题的修复。
- 根目录 `CLAUDE.md` 已改为不携带当前分支、测试数字和历史交付状态的长期规则，明确禁止
  Superpowers/旧验收矩阵/低价值重复测试，保留洋葱分层、账号授权、事务幂等、隐私和精简验证。
  新增全局游标只能推进连续已处理前缀、primary Actor 必须由权威 SourceEvent 和来源集合证明、
  远程 LLM 使用不透明身份引用等评审要求。
- 同时固化 `Main` 分支大小写、数字人与 QQBot 完全隔离、`.mcp.json` 不得触碰，以及每个 QQBot
  提交必须同步 TODO、HISTORY 和月度历史。本次未改业务代码，未运行外部联机，未提交、推送或
  合并。

## 2026-08-01 23:08（Asia/Shanghai）：结构化记忆候选独立复核未获批准

- 独立运行 `cargo fmt --all -- --check`、两个相关 crate 的严格 Clippy、personal-secretary
  223 个领域单测和 qqbot-server 109 个非忽略单测，均通过。随后在
  `serverrs-qqbot-mysql` 创建随机 `qqbot_accept_codex_*` schema，真实重跑候选审批、跨账号/
  版本/来源失效、相同事实来源合并和 local_only 远程隔离 4 条 MySQL 场景，全部通过；临时
  schema 已在 finally 删除，无残留。
- 代码复核发现现有测试未覆盖的 P0：仓储先选游标后的首个会话，再读取该会话所有后续事件并把
  账号级游标推进到最后一个事件；当接收顺序为 `群 A -> 群 B -> 群 A` 时，中间 B 事件会被
  永久越过。另一个 P0 是候选只验证来源 Actor 属于同账号，没有强制其等于被引用 SourceEvent
  的权威 Actor；Person/Commitment 的 primary actor event 也不要求包含在来源集合，批准事务又
  信任候选来源表的 Actor，可能产生证据与身份不一致的 Confirmed Fact。
- 同时记录三项 P1：远程 LLM 输入不应包含原始会话/QQ 稳定标识；来源正文投影缺失时当前失效
  SQL 会因 INNER JOIN 无行而漏掉候选；`approve_conflict` 的版本 CHECK 允许落入通用 `+1`
  分支，约束比注释和状态机更宽。上述事项已写回 TODO，修复前 `MEM-011` 不得提交或宣称完成。
  本次未连接 NapCat/QQ 开放平台、未发送消息、未修改数据库权限，也未提交、推送或合并。

## 2026-08-01 19:41（Asia/Shanghai）：Owner 待办完成与回复期待关闭

- 新增 `CompleteFollowUp`、`CompleteFollowUps`、`DismissResponseExpectation` 和
  `DismissResponseExpectations` 四种类型化 L2 Action。每批最多 20 个明确 ID/来源版本，
  继续复用持久化 Suspend/重启 Resume、Checkpoint 单次消费、唯一 OwnerBinding 和账号验权。
- FollowUp 完成在一个事务中执行 `scheduled -> completed`、来源版本精确加 1、due 不变，并
  压制尚未投递通知；ResponseExpectation 关闭执行 `active -> dismissed`，不伪造代表真实回复
  的 `resolved`，也不修改 EventThread/OpenQuestion。单条与批量均先锁定并验证全部目标和关联
  Outbox，再写逐目标不可变审计与整次 Effect Receipt，任一冲突整批回滚。
- 原 FollowUp MySQL 仓储已按授权、业务控制、Outbox 和审计职责拆分；回复期待控制复用同一授权
  与回执边界，不复制一套身份验证。独立复核修正四种关闭动作被错误标记为 `reversible=true`
  的产品语义并增加聚焦回归测试。6 条随机隔离 MySQL 新旧闭环、218 个领域单测、105 个服务
  单测、严格 Clippy 和 19 项应用边界检查均通过，临时 schema 已清理。未运行旧验收矩阵，未
  连接 NapCat/QQ 开放平台，也未发送 QQ 消息。

## 2026-08-01 17:44（Asia/Shanghai）：Owner 批量推迟 FollowUp

- 新增 `SnoozeFollowUps` 类型化 L2 Action，一批接受 1～20 个明确 FollowUp ID/来源版本并
  共用一个新到期时间；不同时间继续拆成多个 Action。Planner、领域门与执行用例共用目标去重、
  版本和数量约束，缺少关键字段时不从正文猜测。
- 最终 MySQL 事务按 FollowUp ID 与通知 ID 的确定顺序加锁，以数据库时钟验证新时间为未来、
  不超过 365 天且晚于所有目标当前 due。任一目标或 Outbox 不安全即整批回滚；成功时全部保持
  scheduled、due 更新、版本精确增加 1，旧 pending/failed 通知被压制，每目标写一条 snooze
  审计，整批只写一条 Effect Receipt。新 due 后各目标以新版本重新生成通知。
- 随机隔离 MySQL 分别验证批量推迟原子性与通知再生、单条推迟回归、批量忽略回归，三个临时
  schema 均已清理；Planner 聚焦测试、严格 Clippy 和 19 项应用边界测试通过。未执行旧验收
  矩阵，未连接 NapCat/QQ 开放平台，也未发送 QQ 消息。

## 2026-08-01 17:24（Asia/Shanghai）：Owner 批量忽略 FollowUp

- 新增 `DismissFollowUps` 类型化 L2 Action，一批只接受 1～20 个明确 FollowUp ID 与 Owner
  所见来源版本，重复 ID、零版本或缺失关键字段在进入数据库前拒绝；继续复用现有 Suspend、
  重启 Resume、Checkpoint CAS 和 OwnerBinding 授权边界。
- 专用 MySQL 事务按 FollowUp ID 固定顺序锁定并验证全部目标，再按通知 ID 固定顺序锁定
  legacy 与 policy-owned Outbox。任一目标已变化或存在 claimed/unknown_commit 通知即整批
  回滚；成功时全部更新为 dismissed、版本精确增加 1、压制 pending/failed 通知，并按目标写
  不可变审计、按批次写一条 Effect Receipt。审计 ID 使用无歧义字段边界稳定派生。
- 随机隔离 MySQL 分别验证批量成功与错误版本全回滚、单条忽略回归、单条推迟回归，三个临时
  schema 均已清理；Planner 两条聚焦测试、严格 Clippy 和 19 项应用边界测试通过。未执行旧
  验收矩阵，未连接 NapCat/QQ 开放平台，也未发送 QQ 消息。

## 2026-08-01 14:42（Asia/Shanghai）：Owner 推迟单个 FollowUp

- 新增 `SnoozeFollowUp` 类型化 L2 Action，显式携带 FollowUp ID、Owner 所见来源版本、新 UTC
  Unix 到期时间与原因，复用既有 Suspend/重启 Resume/Checkpoint CAS 和专用 FollowUp 控制端口。
- 最终 MySQL 事务以数据库当前时钟复验新时间晚于当前时间、晚于原 due 且不超过 365 天；锁定
  FollowUp 与 legacy/policy-owned 两类 Outbox，精确推进来源版本、压制未投递通知，并原子写入
  含前后 due/版本的不可变审计和 Effect Receipt。claimed/unknown_commit 拒绝，delivered 保留。
- 到达新 due 前不会产生新 Candidate；到达后 source_version=2 的 Candidate/Request 经统一策略
  求值生成新的 Outbox occurrence，旧 suppressed 行保持终态。独立复核发现原测试在 OwnerBinding
  建立前求值，修正 fixture 顺序后随机隔离 MySQL 完整闭环通过，临时 schema 已清理。严格
  Clippy 与 19 项应用边界测试通过；未运行旧验收矩阵，未连接或发送 QQ。

## 2026-08-01 13:45（Asia/Shanghai）：补齐 policy-owned Outbox 压制

- 复核 Task 7 后发现现行 Outbox 通过 Notification Candidate 回溯 FollowUp，行内 legacy
  `follow_up_id` 为空。控制事务现同时锁定并压制 legacy 与 policy-owned 两种 Outbox，避免
  “业务已忽略但现行通知仍待投递”。
- 聚焦 MySQL 测试不再手工插入 legacy Outbox，而是真实执行 Candidate→Evaluation→Decision→
  policy-owned Outbox 后再审批忽略；结果通过，随机 schema 已清理。

## 2026-08-01 13:38（Asia/Shanghai）：Owner 忽略单个 FollowUp

- 新增 `DismissFollowUp` 类型化 L2 Action，以稳定 FollowUp ID 和 Owner 所见来源版本执行
  `Proposal → Suspend → 重启 Resume → Effect → Receipt`，不增加旁路接口或 QQ 写能力。
- 专用 MySQL 事务复验 Action 租约、OwnerCommand、唯一有效 OwnerBinding、账号和版本；将
  scheduled 更新为 dismissed 并精确增加版本，压制 pending/failed Outbox，原子写入控制审计和
  通用回执。claimed/unknown_commit 保守拒绝，delivered 历史保留。
- 独立评审修复三处边界：FollowUp ID 收紧为 36 字节、Proposal JSON 必须与 ID/Action 一致、
  锁定 FollowUp 的全部 Outbox 行以消除检查后被投递 Worker 抢占的竞态。真实隔离 MySQL 首轮
  发现测试 SQL 使用 Rust 数字下划线，次轮发现 unsigned 版本以 i64 解码；修正后完整主路径与
  错误版本回滚通过，临时 schema 已清理。未执行 NapCat 或 QQ 开放平台实机操作。

## 2026-08-01 13:16（Asia/Shanghai）：待处理事项版本 fencing 基础

- `PendingOwnerWorkItem` 增加可选 `source_version`。FollowUp、Agenda 与回复期待返回数据库真实
  版本；没有独立来源版本的 Outbox 返回 `None`，不以零值伪造。
- MySQL UNION 查询保持账号过滤、排序与数量上限不变；Owner 文案只在版本存在时展示
  `version N`，为下一步忽略或推迟单个 FollowUp 提供稳定并发条件。
- 随机隔离 MySQL 验证 FollowUp/Agenda 版本、Outbox 无版本和跨账号不可见。首轮测试暴露 SQL
  中误用 Rust 数字字面量 `90_000`，修正为合法 MySQL 数字后通过；临时 schema 已清理。
  未执行 NapCat 或 QQ 开放平台实机操作。
