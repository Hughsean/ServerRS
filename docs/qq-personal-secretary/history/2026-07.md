# 个人 QQ 智能秘书开发历史：2026-07 归档

> 最后更新：2026-07-31
> 本文件只记录已经发生且有证据的事项；未来计划统一放在 `TODO.md`。

## 2026-07-31 22:09（Asia/Shanghai）：开放问题超时未回复主动跟进

- 新增 `secretary_response_expectations` 迁移与四小时默认超时配置。仅从带精确来源的外部联系人
  开放问题生成期待；候选携带账号、会话、Actor、通知类别和 `response_overdue` 类型化元数据，
  不把语义相似度或任意正文放入策略匹配键。
- 到期后统一走 Candidate → Evaluation Request → Decision → Owner-only Outbox；扫描本身不直写
  Outbox，更不向联系人或群成员发送。本人在同线程的后续输出、问题状态变化或线程终态会结束
  期待，并抑制 pending/failed 通知。
- 验证：相关 crate 编译与严格 Clippy、既有 217 + 98 项测试通过；随机隔离 MySQL 验证完整策略
  与回复失效主路径，临时 schema 已清理。未执行 NapCat 或 QQ 开放平台实机操作。

## 2026-07-31 21:52（Asia/Shanghai）：Owner 记忆控制 Action 闭环

- 新增列出记忆、来源回读、不可变修正、删除派生记忆、设置/取消 TTL、会话长期记忆模式六类
  类型化 Action；只读操作为 L0，写操作为 L2 Suspend/Resume，不提供 SQL、HTTP、文件或任意工具入口。
- Planner、Action Graph 和生产装配接入 `MemoryUseCase`。修正与 TTL 修订以 Effect ID 派生稳定
  Fact ID；删除和会话模式更新均为幂等操作。会话模式写入前在 MySQL 事务内复验 OwnerCommand、
  本地 OwnerBinding、被管理账号与目标会话，禁止跨账号更新。
- 验证：`cargo fmt --all`、相关 crate 严格 Clippy、personal-secretary 217 项、qqbot-server 98 项
  全绿；随机隔离 MySQL 主路径验证记忆来源、授权会话模式切换、幂等重放、删除和 FollowUp 协调，
  临时 schema 已删除。未执行 NapCat 实机、QQ 开放平台连接或消息发送；无数据库迁移。

## 2026-07-29 23:29（Asia/Shanghai）：Recall WAL 恢复韧性验收

- 根因：`acceptance_stalled_recall_store_does_not_block_websocket_and_retries` 的故障注入删除
  `secretary_recall_inbox` 后未移除 `qqbot_test_schema_migrations` 中的对应记录。共享幂等迁移
  加载器正确跳过已记录迁移，所以表没有恢复，Worker 对不存在表持续重试直到验收超时。
- 修复：测试注入器同步删除 `20260727_personal_secretary_recall_inbox.sql` 的记录，使恢复步骤真实
  重建 inbox。生产 Recall WAL/Worker 未改动：回调仅等待加密 WAL append/fsync，Worker 继续按周期
  自动 drain，MySQL enqueue 成功前不 checkpoint。
- 证据：`B3-RECALL-004-RESILIENCE` 实际 PASS（3.837s），完整隔离 MySQL 验收通过；五项
  `NPOLICY-*` 继续 PASS；fmt、严格 Clippy、workspace boundaries（19/19）和 `git diff --check`
  通过。
- 验收脚本兼容本机 PowerShell 5.1/.NET Framework，修正 JSON、SHA-256、相对路径、Cargo stderr、
  Docker port 及隔离 schema 命令调用。每项检查创建的随机 `qqbot_accept_*` schema 均在 finally
  删除，未触及四个既有遗留 schema：`qqbot_accept_20260729142255_697e4cce`、
  `qqbot_accept_20260729214037_978a58d9`、`qqbot_accept_20260729214904_ba276172`、
  `qqbot_accept_20260730000216_ef44aea1`。
- 数据库影响：无迁移、无账号权限变更、无已有或遗留 schema 删除。外部系统影响：未连接真实 QQ、
  QQ 开放平台或第三方，未发送 QQ 消息。未验证：L4/L5 独立 hash-bound attestation 仍缺失，故
  完整 Release Gate 为 `REJECTED`。Git 状态：工作树原有大量未提交改动完整保留；本轮未提交、未推送、
  未合并、未 stash。

## 2026-07-30：全分支差异审查中的策略日志 P1 修复

- 发现新 `notification_policy_worker` 与启动协调失败路径将完整错误对象写入结构化日志；数据库驱动错误可能携带原始错误体或连接细节，不符合日志仅保留脱敏错误码、状态、计数与 backoff 的约束。
- 修复为固定 `error_code` 与 `consecutive_errors`，不再渲染原始 error；启动失败仍向上返回原错误以维持控制流，不将其写入日志。
- 复验：`cargo fmt --all -- --check`、`cargo check -p personal-secretary -p qqbot -p qqbot-server --tests`、策略 Worker 单测、配置测试 33/33（1 项既有 live 测试 ignored）、严格 Clippy 和 `git diff --check` 均通过。未连接真实 QQ、QQ 开放平台或第三方，未发送 QQ，未执行数据库删除、提交、推送、合并或 stash。

## 2026-07-30 10:19（Asia/Shanghai）：验收脚本凭据 P1 关闭

- 隔离 schema 创建和清理的 `docker exec` 命令不再拼入 MySQL root 密码；容器内 shell 仅以
  `MYSQL_PWD="$MYSQL_ROOT_PASSWORD"` 展开认证。宿主机命令行只包含字面变量引用。
- PowerShell 5.1 解析通过；`B3-RECALL-004-RESILIENCE` 使用随机 schema
  `qqbot_accept_20260730101342_6c0e95c5` 的隔离 MySQL smoke 通过，finally 已清理该 schema；
  `git diff --check` 通过。
- 当前盘点为四个既有遗留 schema，均未删除：`qqbot_accept_20260729142255_697e4cce`、
  `qqbot_accept_20260729214037_978a58d9`、`qqbot_accept_20260729214904_ba276172`、
  `qqbot_accept_20260730000216_ef44aea1`。无迁移、无账号权限变更、未连接真实 QQ/第三方，未发送
  QQ 消息；未提交、未推送、未合并、未 stash。

## 2026-07-23：产品方向切换为个人智能秘书

- 形成 V2.0 需求草案：NapCat 作为个人 QQ 全消息感知通道，QQ 开放平台作为 Owner 控制与
  提醒通道，知识库延后。
- 明确 NapCat 在 MVP 中只读，不代表 Owner 自动回复任何联系人或群。
- 明确采用有界结构化状态、最近窗口和按需来源检索，而不是反复重放全部历史。
- 文档状态：需求草案仍位于 Main 根目录且未跟踪，本次未移动或覆盖。

## 2026-07-23：首个统一身份和消息入口切片

- 分支/工作区：`codex/qq-personal-secretary`，隔离工作树
  `.worktrees/qq-personal-secretary`。
- 新增 `crates/personal-secretary`，定义消息来源、账号主体、会话、可信发送者、消息角色和
  幂等键。
- 指令不变量：只有 QQ 开放平台 Owner 控制会话中的已验证 Owner 是 `OwnerCommand`；
  NapCat 中 Owner 自己发送的内容是 `OwnerObservation`。
- NapCat 新增私聊和 `message_sent` 事件；群聊行为保持兼容。
- `qqbot-server` 将 NapCat 消息映射到统一身份边界，不记录消息正文，不发送消息，不写数据库。
- 数据库影响：无迁移、无表变更、无现有数据读写。
- 外部验证：本地 NapCat WebSocket 实机握手成功；未进行真实聊天正文采集。
- 自动验证：个人秘书、QQ 适配器和服务端聚焦测试 24 项通过，架构边界 9 项通过；格式化和
  差异检查通过。
- 已知基线：严格 Clippy 被 `qqbot/napcat/api.rs` 7 个既有 `needless_question_mark` 告警
  阻断；本次范围在仅豁免该规则后通过。
- Git 状态：截至本记录，改动尚未提交或合并。

## 2026-07-23：因果链、主动跟进和结构化记忆审计

- 确认 Reply ID 目前只停留在协议消息段，尚未解析为持久化事件因果边。
- 确认当前没有 SourceEvent、EventThread、Decision、OpenQuestion、Commitment、Scheduler、
  Secretary Memory 或 Owner 自然语言 Action。
- 确认 `agent-core` 的 Graph、Effect、Checkpoint、Suspend/Resume 可以复用，但尚未建立个人
  秘书业务状态和节点。
- 建立 `docs/qq-personal-secretary/` 作为后续 QQ 智能秘书项目文档的正式入口。
- 建立能力审计、分阶段 Todo 和本历史记录；下一开发切片确定为可靠事件存储。

## 2026-07-23：独立配置与可靠消息持久化切片

- QQBot 配置入口独立为 `apps/qqbot-server/config/qqbot.toml`，示例和本地 `.env` 也只位于
  该目录；仅使用 `QQBOT_CONFIG_PATH`、`QQBOT_DATABASE_URL` 和 QQBot/NapCat 专属变量。
- QQBot 数据库迁移独立位于 `apps/qqbot-server/database/migrations`；新增四张
  `secretary_*` 表，不修改数字人的 `database/sql/init.sql` 和迁移目录，不复用旧 `qq_*` 表。
- 新增协议无关的消息内容段、@成员、@全体和 Reply 建模，并将 NapCat 载荷映射到个人秘书域。
- 新增 `InboundEventStoreT` 与 SeaORM/MySQL 实现；消息按“来源账号 + 平台消息 ID”幂等落库，
  同一平台消息 ID 在不同账号主体下保持独立。
- `qqbot-server` 改为先落库：新事件才允许进入后续处理，Duplicate 不重复处理。
- Docker MySQL 8 隔离验收通过：首条插入、重复去重、Reply 因果边、@成员、@全体和跨账号
  隔离均验证成功。
- 未完成：连接周期、Cursor、Gap、历史回补、有界队列/背压、Reply 待回填和非消息事件。
- Git 状态：截至本记录，改动仍位于隔离工作树，尚未提交或合并。

## 2026-07-23：连接周期、游标与不确定空窗切片

- 新增协议无关的 `ConnectionEpochId`、连接状态/结束原因、`IngestionCursorScope` 和
  `IngestionGapStatus`；连接健康和历史完整性是两个独立状态。
- 新增独立连续性迁移，建立连接周期、实时事件来源关联、账号/会话游标和空窗四张
  `secretary_*` 表；没有修改数字人数据库或根 `init.sql`。
- NapCat WebSocket 握手成功前先创建连接周期；握手成功后持久化 `connected`，消息落库时
  在同一事务更新连接最后事件、账号游标和会话游标。
- 远端断开、传输失败和进程退出都会结束周期；曾经成功连接的周期创建
  `IngestionGap(status=uncertain)`。下次连接只写入空窗结束时间，不会把状态改成已补齐。
- QQBot 的 SeaORM 运行时启用 Rustls；隔离 MySQL 使用 `ssl-mode=required` 完成 TLS 验收。
- Docker MySQL 8 验收：两份迁移可重复执行，消息幂等/Reply/@ 测试与连接周期/双层游标/
  空窗幂等测试连续两轮通过。
- 未完成：NapCat 历史 API 契约、回补 Worker、Gap 完整性判定、有界队列/背压和本地 Spool。
- 平台门槛：进入 QQ 开放平台连接阶段前必须先通知用户并确认凭据只保存在 QQBot 本地配置。
- Git 状态：截至本记录，改动仍位于隔离工作树，尚未提交或合并。

## 2026-07-23：NapCat 历史接口只读收口与契约探测

- 对照 NapCat 官方接口文档和本机在线实例，验证群聊/私聊历史接口可返回标准消息、发送者、
  `message_id` 和 `message_seq` 字段；验证过程没有输出或保存实际聊天正文及联系人 ID。
- 新增 `get_group_msg_history`、`get_friend_msg_history` 和 `get_msg` 类型化读取方法；ID 全部
  按字符串解析，历史单页限制为 1–100。
- 从 NapCat HTTP 客户端删除个人账号发送、戳一戳等修改能力，并增加源码架构守卫。
- 发现重复探测可能返回成功空页；因此只完成读取适配器，尚未启动自动回补，也没有把任何
  `IngestionGap` 标记为 `verified_complete`。
- 新增 `napcat-history-contract.md`，记录官方契约、本机证据和锚点/分页/PacketBackend 未决项。
- QQ 开放平台仍未开始接入；到达该阶段前继续执行“先通知用户、再配置本地凭据”的门槛。

## 2026-07-23：有界入站队列、背压与可调试日志切片

- `qqbot-server` 的 NapCat 回调改为只做类型映射和非阻塞 `try_send`；MySQL 幂等写入从
  WebSocket 回调移到独立有界 `mpsc` Worker。
- Worker 对临时数据库错误执行有上限的指数退避，单条消息成功或确认为重复后再消费下一条；
  无效事件停止重试并留下可审计错误。
- 队列满时不阻塞 WebSocket，聚合丢弃计数并为当前连接周期幂等创建
  `IngestionGap(status=uncertain, reason=queue_overflow)`，不把无法证明的连续性伪装成完整。
- 连接结束时限时排空 Worker；超时会中止内存队列并依赖不确定空窗与后续历史回补。当前尚无
  本地磁盘 Spool，因此数据库长期离线叠加进程崩溃仍可能丢失尚未落库的事件。
- 新增独立 QQBot 队列容量、重试初始/上限和退出排空超时配置；不读取数字人配置。
- 新增 `trace/debug/warn/error` 结构化日志，覆盖入队、持久化尝试、幂等结果、重试、溢出 Gap
  和 Worker 排空；日志不记录聊天正文或凭据。
- 数据库影响：没有新增迁移或修改数字人表；复用 QQBot 自己的连续性表记录溢出 Gap。
- 验证：个人秘书/QQ 适配器/QQBot 服务端聚焦测试 31 项通过；隔离 MySQL 真实集成测试 2 项
  通过；跨业务架构边界 13 项通过；严格 Clippy、格式和差异检查通过。数据库重试恢复、队列满
  立即返回和容量配置上下限均有自动测试。
- QQ 开放平台仍未开始接入。

## 2026-07-23：双 NapCat 实例只读契约复验

- 两个本机 NapCat 实例均验证为不同的在线测试账号；双方互为好友，并同时属于指定测试群。
- 通过 WebUI 内部临时调试适配器对两个账号执行只读 OneBot 调用；没有发送消息、撤回、退群、
  修改网络配置或输出聊天正文及真实账号 ID。
- 双方的好友列表、群列表、群成员、群历史和私聊历史读取均成功；群历史重复读取保持稳定。
- 精确 `message_seq` 是包含式锚点；不存在的相邻序号返回失败，不能用序号加减实现可靠翻页。
- `get_msg` 在消息所属账号内往返成功，跨账号查询同一消息 ID 失败；这为“来源账号 + 平台消息
  ID”幂等键提供了实机证据。
- 新增忽略型 `napcat_live` 契约测试；第一实例的真实 OneBot HTTP 读取和 WebSocket 握手 2 项
  通过。第二实例尚未配置 OneBot HTTP/WebSocket 服务器。
- 第一实例当前关闭本人消息上报；本轮测试群/私聊历史样本较少且只有文本段，因此 @、Reply、
  本人消息、撤回、断连和多页稳定翻页仍不能标记完成。
- 数据库影响：无。QQ 开放平台仍未开始接入。

## 2026-07-23：双账号测试群主动契约与真实入库验收

- 获得仅在指定测试群发送主动测试消息的授权；测试代码没有 `send_private_msg`，没有向好友或
  其他群发消息。
- 临时为第一实例开启本人消息上报，并为第二实例增加独立 WebSocket；每次运行都先保存完整
  配置，`finally` 清理后重新读取验证，两份配置均与原快照逐字一致。
- 新增忽略型 `napcat_active_group_live` 契约测试，覆盖未 @、明确 @、本人消息、Reply、撤回
  通知、Listener 重建、双账号历史、`get_msg` 和精确历史锚点；最终 1 项实机测试通过。
- 发现并验证 NapCat 消息 ID 按观察账号分域：回复方必须使用自己收到的父消息 ID，接收方的
  `Reply` 会转换为其账号视角的 ID。双账号各 18 条历史样本的消息 ID 交集为 0。
- 撤回通知已确认存在，但当前 Listener 只记录“尚未建模”的 Debug 日志；该缺口登记为
  `EVT-008`，没有把撤回误报为已经持久化。
- 启动真实 `qqbot-server` 连接一次性 MySQL 8：8 张 QQBot 自有 `secretary_*` 表成功迁移；
  四条测试群消息全部入库，其中 Owner 1、External 3、@ 1、Reply 1，Reply 父事件解析成功，
  四条均关联连接周期，账号/会话两级游标均存在。
- 运行期使用 `trace/debug/info/warn` 结构化日志；日志未记录聊天正文、访问令牌或 WebUI 凭据。
- 最终回归：个人秘书/QQ 适配器/QQBot 服务端 31 项通过，跨业务隔离守卫 13 项通过；严格
  Clippy、格式化和差异空白检查通过。顺带等价化简 6 处只读 API 的冗余 `Ok(...?)`。
- 所有主动测试消息均通过发送账号撤回，真实服务进程已停止，NapCat 配置已恢复；一次性
  MySQL 容器和本次主动验收构建目录均已删除。
- QQ 开放平台仍未开始接入。

## 2026-07-24：历史回补生命周期与潜在故障加固

- 分支/工作区：`claude/qqbot-history-backfill`（同一工作树，在 2026-07-23 切片之上修复）。
- 背景：2026-07-23 切片被过度声明 DONE；端到端走查和系统性审计发现多项 Gap 生命周期、
  调度、租约并发、身份隔离与隐私错误路径问题，本次集中修复并补齐失败测试。
- 完成范围：
  - #1 开放空窗不可回补：`claim_next_gap` SQL 增加 `gap_ended_at IS NOT NULL`。
  - #2 uncertain Gap 可重入不饿死：移除 `secretary_backfill_runs.gap_id` 唯一键；新增
    `secretary_gap_reclaim_schedule(next_eligible_at)`；`Unprovable` 退避后可重试，
    `KnownScopesComplete` 自动挂起，只在人工重验或来源能力升级后重新排队；
    `ORDER BY updated_at ASC` 防止饿死后续 Gap。
  - #3 边界快照 + message_id 匹配：新增 `secretary_gap_boundaries`，`finish_connection`/
    `mark_connection_uncertain` 创建 Gap 时用 `INSERT...SELECT ON DUPLICATE KEY`（首写获胜）
    冻结会话游标；`known_scopes_for_gap(gap_id)` 读快照；用例边界按 message_id 匹配。
  - #4 Worker 关闭可退出：`BackfillHandle` 加取消状态，领取等待和在飞任务均监听关闭；
    关闭后有界清理并中止卡住任务。
  - #5 真实并发：引入 `BackfillRunner` trait，`scan_once` 用 `tokio::task::JoinSet` 真正
    spawn `max_concurrency` 个任务。
  - #6 租约 CAS + 配置化 + 错误分类：`reclaim_expired` 加 `FOR UPDATE`+CAS；`MySqlInboundEventStore`
    持有 `lease_secs`（`build_mysql_backfill_store(db, lease_secs)`）；`map_source_error` 不再因
    retcode=200 判 `PermissionDenied`（改 Protocol 暂时性）。
  - #7 恢复调度：把“领取过期运行”和“执行历史恢复”拆开，领取数量受并发上限约束，恢复
    进入统一 `JoinSet`；移除会误杀合法长恢复的固定 5 秒超时。
  - #8 错误退避：第一次错误使用 `retry_initial_ms`，随后指数增长并封顶；退避是下次扫描前
    不可绕过的最短等待，不再被 30 秒 ticker 或重连 wake 提前打断。
  - #9 租约 fencing：新增 `secretary_backfill_leases` 所有权表；首次领取和每次过期接管均
    轮换令牌，远程读取前续租验权，进度写入和终态提交必须匹配当前令牌；旧 Worker 迟到
    写入返回 `LeaseLost`，同令牌的已提交终态重试仍幂等成功。令牌独立建表，使重复执行迁移
    可以为已应用早期草稿 runs 表的环境补齐 fencing，不依赖不受 MySQL 支持的幂等 ADD COLUMN。
  - #10 有界恢复：崩溃恢复从持久化 Scope 进度恢复已消耗事件数，不再把
    `max_events_per_run` 预算重置为 0。
  - #11 身份与隐私：历史 Scope、响应 `self_id` 和群 `group_id` 必须一致；缺失稳定
    message ID/sequence 的页面不再静默跳过；NapCat 失败响应的完整 `data` 不再传播到日志或
    Scope 证据。
- 数据库影响：修改未合并迁移 `20260723_personal_secretary_backfill.sql`（移除唯一键、新增
  `secretary_gap_boundaries`、`secretary_gap_reclaim_schedule` 和
  `secretary_backfill_leases`）；不修改
  数字人迁移或根目录 `init.sql`。
- 验证：personal-secretary/qqbot/qqbot-server 常规测试 72 项通过；隔离 MySQL 8.4 集成测试
  12 项全通过，覆盖迁移重复执行、开放 Gap、退避/挂起、成功路径、并发过期接管、令牌轮换、
  旧令牌拒绝与终态幂等重试；一次性 MySQL 容器已停止并删除。严格 Clippy、架构边界和最终
  差异检查见本次交付前复验结果。
- 未完成：真实 NapCat 实机多页/跨重启验证仍未做（测试用 Fake 来源）；本地 Spool（GAP-007）、
  撤回投影（EVT-008）不在范围。
- Git 状态：改动仍位于隔离工作树，尚未提交或合并。

## 2026-07-23：历史回补 Worker 与 Gap 完整性证据闭环

- 分支/工作区：`claude/qqbot-history-backfill`，隔离工作树
  `.worktrees/qqbot-history-backfill`，基线 `ad343a8`。
- 完成范围：
  - 领域层（`crates/personal-secretary/src/backfill.rs`）：协议无关的回补模型
    `BackfillRunId`/`BackfillScope`/`BackfillCursor`/`BackfillAnchor`/`BackfillBudget`/
    `BackfillEvidence`/`BackfillOutcome`/`HistoryCompleteness`、Gap 状态机转换规则
    `validate_gap_transition`、完整性证据判定、预算校验。不依赖 NapCat/SeaORM/Tokio。
  - 应用层（`crates/personal-secretary/src/backfill_service.rs`）：端口
    `HistoryBackfillSourceT`/`BackfillStateStoreT`/`BackfillStateStoreWithIngestionT` 与
    `BackfillGapUseCase` 编排（领取 Gap、读已知 Scope、有界分页、统一幂等入口、保存进度
    与证据、决定 Gap 状态转换）。
  - MySQL 仓储（`crates/personal-secretary/src/infra/repo/mysql_backfill.rs`）：原子领取
    `uncertain -> backfilling` + 租约、`reclaim_expired` 崩溃恢复、`known_scopes_for`、
    `record_scope_progress`、`load_run_progress`、`finalize_run`。
  - Reply 子先父后回填（`mysql_inbound.rs`）：父消息入库时同账号批量回填未解析的
    `reply_to_event_id`，幂等且不跨账号。
  - 迁移 `apps/qqbot-server/database/migrations/20260723_personal_secretary_backfill.sql`：
    `secretary_backfill_runs`/`secretary_backfill_scopes`，可重复执行，外键/索引/回滚说明。
  - NapCat 历史适配（`apps/qqbot-server/src/backfill/napcat_history_source.rs`）：实现
    `HistoryBackfillSourceT`，复用 `NapCatInboundMapper::map_core` 使历史与实时同一套身份
    规则；锚点绑定账号视角，不跨账号、不数值加减。
  - 独立 Worker（`apps/qqbot-server/src/backfill/worker.rs`）：与实时 WebSocket 解耦，周期
    扫描 + 重连唤醒，`max_concurrency` 信号量限制，结构化日志含 run_id/gap_id/completeness/
    pages/accepted/duplicates，不记录正文/Token/完整响应。
  - 配置（`apps/qqbot-server/src/config.rs`）：`[backfill]` 段与 `QQBOT_BACKFILL_*` 环境变量，
    `page_size∈1..=100`、`max_concurrency∈1..=64`、`retry_max≥retry_initial` 等校验。
- 未完成范围：本地磁盘 Spool（GAP-007）、账号级会话集合完整性证明（真实 NapCat 不可证）、
  撤回事件投影（EVT-008）、实机多页方向与跨重启翻页稳定性。
- 数据库影响：新增前向迁移 `20260723_personal_secretary_backfill.sql`，建立两张
  `secretary_backfill_*` 表；不修改数字人库或既有 `secretary_*` 表；回滚顺序见迁移末尾。
- 外部系统影响：NapCat 仍只读；未发送/撤回/修改配置；无新主动测试授权。
- 验证：`cargo fmt --all -- --check` 通过；`cargo test -p personal-secretary -p qqbot
  -p qqbot-server` 通过（personal-secretary 26、qqbot 15、qqbot-server 20，MySQL/live
  忽略型测试未运行）；`cargo clippy -p personal-secretary -p qqbot -p qqbot-server
  --all-targets -- -D warnings` 通过；`cargo test -p digital-human-server --test
  workspace_boundaries` 16 项通过。MySQL 隔离集成测试（迁移顺序/重复、原子领取/租约恢复、
  幂等、Reply 回填、跨账号隔离、用例+真实 MySQL）为忽略型，需 `QQBOT_TEST_DATABASE_URL`。
- 文档变化：更新 TODO（GAP-004/005 DONE、EVT-007 PARTIAL 补充子先父后）、HISTORY、
  napcat-history-contract、capability-assessment、crate-boundaries、database/config README。
- 下一项与阻塞：因果线程层（THR-*）；账号级会话完整性需更多实机证据；本地 Spool 待评估。

## 2026-07-24：确定性 EventThread 批量投影切片

- 分支/工作区：`claude/qqbot-history-backfill`，继续在隔离工作树追加，未提交、未合并。
- 完成范围：
  - 领域层新增 `EventThread`、`ThreadClaim`、`ThreadDecision`、`OpenQuestion`、
    `ThreadRelation`、生命周期和来源证据类型；不依赖 NapCat、MySQL 或 Tokio。
  - `DeterministicThreadPlanner` 对有界批次执行 Reply 父链优先、同会话短窗口聚合和新线程
    兜底；发送者只形成同会话窗口内的附加关系，绝不单凭同一发送者跨群自动合并。
  - 应用层新增 `ThreadProjectionStoreT` 与 `ThreadProjectionUseCase`，一次领取、一次规划、
    一次事务提交；默认路径不调用 LLM，也不重放完整消息历史。
  - MySQL 新增线程、线程成员、确定性关系和独立投影租约四张 `secretary_*` 表；投影消费者
    不复用 `source_events.processing_status`，租约过期可接管，提交前执行令牌验权。
  - `qqbot-server` 新增独立批量 Worker、QQBot 专属配置、扫描上限、指数退避、可取消关闭和
    `trace/debug/warn` 日志；日志不记录聊天正文。
- 未完成范围：明确项目 ID/文件版本/非 Reply 引用的结构化提示、要求/反对/确认提取、结论
  修订链、线程生命周期推进、跨会话候选链接、隐私策略和人工拆分/合并仍属于 THR-002/004～010。
- 数据库影响：新增前向迁移 `20260724_personal_secretary_threads.sql`，只位于 QQBot 独立迁移
  目录，不修改数字人 `init.sql` 或数字人表；回滚顺序写在迁移末尾。
- 外部系统影响：未调用 NapCat HTTP/WS 发送能力，未发送消息，未连接 QQ 开放平台；用户报告
  NapCat `127.0.0.1:3000`/`127.0.0.1:6700` 已启动，本切片无需修改其配置。
- 验证：领域 Reply/窗口/跨群/时间戳漂移测试通过；Worker 卡住关闭测试通过；一次性 MySQL
  8.4 中 13 项集成测试串行全通过，覆盖四消息两线程、跨窗口 Reply、同群近邻、跨群隔离、
  空扫描和既有回补回归；测试容器与临时数据已删除。
- 下一项与阻塞：THR-004 类型化提取需要先定义候选补丁和来源引用，再决定是否接入 LLM；
  THR-002 的项目 ID/文件版本必须来自结构化提示，不能以同名文本直接自动合并。

## 2026-07-24：线程类型化语义、修订链与生命周期安全底座

- 完成范围：新增 `ThreadSemanticExtractorT`/`ThreadSemanticStoreT`/`ThreadSemanticUseCase`；
  提取器只返回类型化候选补丁，不能直接写库或改变线程终态。
- 当前保守提取器按线程有界批次识别明确的请求、反对、确认、决定和问句；所有候选默认
  `proposed`，模糊聊天不生成事实，“请问”不会误判为动作请求。
- 安全校验：候选必须引用本批次原始事件、参与者必须完整匹配已观察身份、置信度/文本/
  候选数/来源数均有上限；超字符预算正文整条跳过推断，不使用截断文本。
- 结论修订：新增 `supersedes_id` 唯一链，候选只能引用同线程 confirmed 结论，不允许新总结
  静默覆盖旧结论。
- 生命周期：实现 `open/waiting/resolved/closed/reopened` 合法转换和不可变来源历史；关闭必须
  有已验证 `OwnerCommand` 来源且不存在开放问题。
- 数据库影响：新增独立迁移 `20260724_personal_secretary_thread_semantics.sql`，增加语义游标/
  租约及候选、来源、修订、问题、状态历史共 9 张 `secretary_*` 表；数字人数据库不变。
- 隐私：`never_long_term` 会话及 `envelope_only` 正文不进入语义提取；完整跨会话检索授权和
  既有派生数据清理仍属于 THR-009 后续范围。
- 运行时：新增独立语义 Worker、QQBot 专属配置、扫描/字符/事件预算、错误退避、可取消关闭
  和无正文结构化日志；不调用 NapCat 发送接口，也不接入数字人 LLM。
- 验证：常规领域/服务测试、严格静态检查和架构守卫见交付结果；一次性 MySQL 8.4 中 14 项
  集成回归串行全通过，包含 26 表迁移幂等、候选/来源持久化、隐私过滤和旧回补链；容器已删除。
- 未完成：自由文本上下文/指代提取、Owner 确认/撤销控制面、自动结束条件、跨会话候选链接和
  人工拆分/合并仍待 THR-004～010 后续切片。

## 2026-07-24 14:10（Asia/Shanghai）：跨群/私聊线程关联候选闭环

- 分支/工作区：`claude/qqbot-history-backfill`，继续位于隔离工作树；本节与代码均尚未提交或
  合并，因此 Main 工作区暂时不可见。
- 完成范围：新增 `ThreadLinkSignalKind`、`ThreadLinkHint`、`ThreadLinkCandidate`、来源证据和
  候选状态；领域校验仅允许明确项目 ID、精确文件 `source_key` 两类强证据生成候选，并要求
  账号一致、线程/会话不同、线程对规范排序、理由与证据类型一致。
- 安全边界：同名人物、相似话题和相同文件名被显式建模为弱证据，不能通过领域校验；候选
  固定为 `proposed`，本切片从不修改 `secretary_thread_events`，不会自动合并线程。Owner
  查看、接受、拒绝和拆分操作仍留给后续控制面。
- 隐私：提示值仅以 SHA-256 指纹保存，不复制项目编号、文件源键或文件名明文；扫描继续排除
  `never_long_term` 会话和 `envelope_only` 正文。日志只输出批次数量、候选数和耗时，不输出
  聊天正文或提示值。
- 应用/运行时：新增有界 `ThreadLinkUseCase`、独立 MySQL 租约/幂等状态和
  `[thread_links]` Worker；支持事件/字符/批次数上限、租约过期接管、指数退避、卡住任务可取消
  关闭及 `trace/debug/warn` 调试日志。
- 数据库影响：新增 QQBot 独立迁移 `20260724_personal_secretary_thread_links.sql`，建立扫描状态、
  强提示指纹、关联候选和候选来源 4 张 `secretary_*` 表；不修改数字人 `init.sql` 或数字人表。
  回滚按来源表、候选、提示、扫描状态顺序人工执行。
- 验证：常规测试 personal-secretary 42 项、qqbot 15 项、qqbot-server 36 项通过；一次性 MySQL 8.4 隔离容器
  中 15 项集成回归串行全通过，包含跨群/私聊强证据候选、双方来源、相同文件名不关联、隐私
  会话不派生，以及全部既有回补/投影/语义测试。首次验收发现 `COUNT(*)` 有符号解码差异，已
  修正后复验；测试容器和临时数据已删除。
- 外部系统影响：只做了 `127.0.0.1:3000` HTTP 端口和 `127.0.0.1:6700` WebSocket 端口的
  TCP 可达性检查，均可连接；未调用 NapCat 主动消息接口，未发送任何 QQ 消息；未连接 QQ 开放平台，也未
  创建或读取 App ID、Secret、Owner OpenID。进入 CMD-001/CMD-002 前仍必须先明确通知用户，
  等待确认后才把凭据写入 Git 忽略的 QQBot 本地配置，绝不写入文档、日志或提交。
- TODO 变化：THR-007 标记 DONE；THR-002、THR-008、THR-009 更新为当前真实完成边界。
- 下一项与阻塞：优先补 Owner 候选查看/接受/拒绝控制入口与错误关联拆分；这将进入 CMD
  控制面和 QQ 开放平台接入门槛，需要先通知用户并等待本地凭据确认。

## 2026-07-24 14:19（Asia/Shanghai）：Owner 关联审核与跨主体授权闭环

- 完成范围：新增 `ThreadLinkReviewUseCase`，支持按账号和稳定游标分页查看关联候选、双方会话、
  有界原文片段、置信度及理由；支持接受/拒绝并返回幂等 Receipt。
- 权限模型：审核命令必须来自 QQ 开放平台的 `OwnerCommand`，并通过本地
  `secretary_owner_bindings` 显式绑定到被管理的 NapCat 账号。普通观察、未绑定 Owner、绑定到
  其他账号的命令均拒绝；授权绝不从聊天文本、昵称或相同 QQ 数字猜测。
- 并发/审计：候选行在事务内 `FOR UPDATE`，状态以 `proposed` CAS 更新；每个候选和命令事件
  最多一条不可变审核记录。相同命令重试返回原 Receipt，不同终态竞争失败。
- 非自动合并：`accept` 只把候选标记为 `accepted`，不改写 `secretary_thread_events`，跨群和
  私聊消息仍保留各自线程。显式搬移、拆分和合并继续属于 THR-010 后续高影响操作。
- 数据访问约定：普通 CRUD 后续优先使用 SeaORM Entity/Query；当前显式 SQL 仅保留在 `infra`
  适配器，并用于 `FOR UPDATE SKIP LOCKED`、租约 fencing、MySQL upsert、跨表隐私过滤和 CAS
  等 ORM 难以准确表达的并发语义，领域层与应用层不依赖 SQL。
- 数据库影响：在未合并的新迁移 `20260724_personal_secretary_thread_links.sql` 中追加
  `secretary_owner_bindings` 和 `secretary_thread_link_reviews`；不保存 App ID/Secret，不修改
  数字人数据库。
- 验证：领域/应用常规测试 personal-secretary 43 项、qqbot-server 36 项通过；隔离 MySQL 8.4
  中 15 项集成回归串行全通过，覆盖未绑定/跨账号/非命令拒绝、来源分页、接受、重复幂等、
  单条审计和不自动合并；一次性容器与数据已删除。
- 外部系统影响：未连接 QQ 开放平台，未配置或读取任何真实凭据，未调用 NapCat 主动消息接口。
- TODO 变化：THR-008、THR-010、CMD-003 更新为 PARTIAL 的真实完成边界。
- 下一步：先补稳定 CRUD 的 SeaORM Entity，并实现高影响线程拆分/合并的 Suspend/Resume 影响
  预览；真正进入 CMD-001/CMD-002 或连接 QQ 开放平台前，必须先通知用户并等待本地凭据确认。

## 2026-07-24 14:33（Asia/Shanghai）：历史分块、SeaORM CRUD 与线程变更 Suspend 预览

- 文档治理：根 `HISTORY.md` 改为当前阶段、最近事件和归档导航；既有 349 行原文迁入
  `history/2026-07.md`。归档达到 500 行或 100 KiB 后按日期/业务切片继续拆分，旧记录没有
  可信分钟时不猜测回填。
- 数据访问：新增线程关联候选、审核 SeaORM Entity；审核记录查询/插入和候选 CAS 更新迁到
  SeaORM Query/ActiveModel。`FOR UPDATE SKIP LOCKED`、Owner 绑定授权联表、指纹匹配和 MySQL
  upsert 等并发/方言语义继续封装在 `infra` 显式 SQL 中。
- 高影响动作：新增 `ThreadMutationImpact`、`ThreadMutationApprovalRequest` 和纯领域校验；
  Merge 仅允许 2～10 个不同线程，Split 只允许一个来源线程，事件/会话计数、理由和来源样本
  均有上限且禁止重复 ID。
- Agent Runtime：`suspend_thread_mutation_for_approval` 直接返回类型化
  `NodeResult::Suspend`，原因为 `Approval`；Suspend 结果不携带 Effect，因此 Owner 确认前绝不
  移动消息或写入线程成员。
- 数据库影响：没有新增迁移表；复用未合并的候选/审核表。SeaORM Entity 只映射稳定 CRUD，
  不改变现有数据结构。
- 验证：personal-secretary 45 项、qqbot-server 36 项通过；隔离 MySQL 8.4 中 15 项集成回归
  全通过，覆盖 SeaORM 审核 CRUD、Owner 绑定、幂等审核及既有回补/线程链；Agent Runtime
  64 项和架构边界 16 项通过；完整依赖严格 Clippy、格式和差异检查通过；一次性容器已删除。
- 未完成：Proposal/Checkpoint 持久化、Resume 输入验权、确认后的 Effect 执行和 UnknownCommit
  处理尚未实现，当前只完成“安全地产生影响预览并暂停”。
- 外部系统影响：未连接 QQ 开放平台，未读取或配置真实凭据，未发送 NapCat 消息。
- TODO 变化：THR-010、CMD-008 更新为 PARTIAL。

## 2026-07-24 16:24（Asia/Shanghai）：线程变更持久化 Checkpoint/Suspend/Resume 与逻辑执行闭环

- 分支/提交：`claude/qqbot-history-backfill` 隔离工作树；本次未提交、未合并 Main，保留用户的
  未跟踪 `cc.md`。
- Agent 闭环：新增 `ThreadMutationAgentState`、类型化 ResumeInput/Update/Effect/Receipt、审批
  节点、决策节点和 EffectExecutor。Proposal 先持久化，再由节点主动
  `NodeResult::Suspend(Approval)`；拒绝产生零 Effect，批准只产生一个 Effect。
- 持久化恢复：新增 QQBot 独立 MySQL CheckpointStore，保存纯数据 AgentCheckpoint；进程重启后
  可由新 Runtime 实例加载并以 CAS 单次消费。Graph/State 版本、RunId、位置、预算、Receipt 和
  Suspend 数据均由 Agent Runtime 原协议校验。
- 权限与幂等：Resume 命令必须是 QQ 开放平台 `OwnerCommand`，且命令账号/Owner 与被管理
  NapCat 账号存在 active 本地绑定。普通 NapCat Observation、跨账号和不同决策默认拒绝；
  Effect ID 全局唯一，同一 Effect 重复执行返回 `changed=false`，`UnknownCommit` 禁止自动重放。
- 线程语义：Merge 第一条线程为 canonical，通过可撤销 alias 生效；Split 只允许真子集并生成
  新逻辑线程，通过事件 override 生效。`secretary_effective_thread_events` 已接入后续 Reply
  投影、语义提取和跨会话关联扫描；原始 `secretary_thread_events` 不物理搬移。新消息回复已被
  合并 alias 时，直接投影到 canonical thread。
- 数据库影响：新增迁移 `20260724_personal_secretary_thread_mutations.sql`，包含 Proposal、
  Checkpoint、Merge Alias、Split Override 和有效线程视图；仅位于 `apps/qqbot-server/database`，
  不修改数字人 `init.sql` 或数字人表。回滚顺序已写入迁移。
- 验证：personal-secretary 47 项常规测试通过；严格 Clippy 通过；全新隔离 MySQL 8.4 中 16 项
  串行集成回归通过，覆盖迁移幂等、未授权拒绝、持久化 Checkpoint 跨 Runtime 恢复、单次消费、
  Merge/Split、原始成员不变、Effect 幂等和合并后 Reply 进入 canonical thread。
- 外部系统影响：未连接 QQ 开放平台，未读取或配置 App ID/Secret/Owner OpenID，未调用
  NapCat 主动消息接口；测试只使用一次性本地 MySQL 容器，验收后容器与测试数据已删除。
- TODO 变化：THR-010、CMD-008 更新为后端闭环完成但控制面/反向操作仍待开发的 PARTIAL。
- 下一项：Owner 撤销/反向线程变更与已确认语义修订迁移，随后进入人物、项目和承诺记忆模型；
  进入 `CMD-001/CMD-002` 前继续执行凭据通知门禁。

## 2026-07-24 16:54（Asia/Shanghai）：线程撤销、语义失效与来源化结构记忆多维批次

- Git 检查点：先将上一批回补、线程投影、Owner 审批和持久化 Suspend/Resume 以
  `995e291 功能：完成QQ秘书回补与线程审批闭环` 提交；用户文件 `cc.md` 未纳入提交。随后
  立即继续本批次，当前批次尚未提交或合并 Main。
- 线程撤销：新增 `ThreadMutationRevertInput/Receipt/UseCase`。只有与被管理账号存在 active
  绑定的 QQ 开放平台 `OwnerCommand` 能撤销已应用 Proposal；撤销记录不可变且按 Proposal
  幂等，停用 Merge Alias 或 Split Override，不删除原始 `secretary_thread_events`。
- 派生一致性：线程应用和撤销均刷新已有强提示的有效 thread_id、让涉及受影响来源且仍为
  `proposed` 的关联候选过期、写入每线程语义失效证据并删除旧语义游标。当前语义读取排除失效
  时间之前的 confirmed decision/open question，Worker 从有效线程事件重新投影；旧记录保留审计。
- 结构记忆：新增 `MemoryFact` 公共包络，以及 Person/Project/Commitment 类型化 Payload；公共
  字段包含账号作用域、subject、状态、万分制置信度、1～100 个 SourceEvent、TTL 和单向
  supersedes 修订引用。履约状态必须带完成来源，Actor 必须处于同一被管理账号。
- 记忆持久化：新增 MySQL MemoryStore，支持幂等追加、同账号/同类型/同 subject 修订、活动
  事实有界查询和批量到期标记。`never_long_term` 会话和 `envelope_only` 内容在事务写边界拒绝，
  完整聊天轨迹不复制到事实 JSON，原始事件仍是事实来源。
- 数据库影响：新增 QQBot 独立迁移 `20260724_personal_secretary_thread_revisions.sql` 和
  `20260724_personal_secretary_memory.sql`；新增撤销审计、语义失效、记忆事实与来源表，不修改
  数字人迁移或表。所有新写入均有账号外键、状态约束、唯一修订/撤销约束和回滚顺序。
- 日志：新增线程撤销、下游失效、结构记忆写入和 TTL 到期的结构化 debug/info 日志，不输出
  正文、凭据或事实 Payload。
- 验证：全新隔离 MySQL 8.4 中 17 项串行集成测试通过，覆盖撤销授权/幂等、有效线程恢复、
  语义失效数量与游标重置，以及人物/项目/承诺来源、修订、TTL、隐私拒绝；personal-secretary
  49 项、qqbot 15 项、qqbot-server 36 项、agent-core 64 项、架构边界 16 项通过，严格 Clippy、
  格式和差异检查通过。
- 外部系统影响：未连接 QQ 开放平台，未配置或读取 App ID/Secret/Owner OpenID，未调用
  NapCat 主动消息接口；隔离 MySQL 容器验收后删除。
- TODO 变化：THR-010 增加撤销/失效完成边界；MEM-001、MEM-005 完成，MEM-002/003/004/006/
  007/009 更新为真实 PARTIAL；OPS-002 增加撤销与记忆日志。
- 下一项：记忆最小来源回读、删除/恢复控制、冲突驱动回读，以及从 Commitment 产生待确认的
  FollowUp 候选和持久化 Scheduler；进入 QQ 开放平台前继续执行凭据通知门禁。

## 2026-07-24 21:00（Asia/Shanghai）：记忆控制、冲突回读与承诺提醒 Scheduler/Outbox 闭环

- 分支/提交：`claude/qqbot-history-backfill` 隔离工作树；安全检查点仍为 `995e291`，本批次与
  16:54 批次尚未提交或合并 Main，用户未跟踪文件 `cc.md` 保持未修改。
- 记忆证据：新增按 Fact ID 的有界来源回读，返回 SourceEvent、会话、Actor、时间和最多 2000
  字符的原文片段；`never_long_term`/`envelope_only` 策略继续阻止长期记忆或正文回读。
- 记忆控制：新增派生记忆删除命令/回执和不可变审计。删除必须引用 QQ 开放平台
  `OwnerCommand`，并通过本地 Owner Binding 授权；只将派生事实标为 `deleted`，不删除原始
  SourceEvent。同一命令幂等，不同删除记录冲突拒绝。
- 冲突回读：同账号/类型/subject 已有活动事实时，新事实不得并列写入，必须先回读来源并显式
  提供 `supersedes_fact_id`；旧事实保持审计并标为 `superseded`。
- 主动跟进：新增承诺 FollowUp Store/UseCase、持久化跟进事项和 Notification Outbox。常驻 Worker
  先有界清理 TTL，再物化确认且有截止时间的承诺；到期只写唯一 `pending` Outbox，不直接发 QQ。
  承诺完成/取消、事实删除/过期/修订后，事项终止且尚未发送的通知标为 `suppressed`。
- 运行保障：新增 QQBot 独立 `[follow_up]` 配置和 `QQBOT_FOLLOW_UP_*` 覆盖，支持批量/视野/
  扫描间隔上限、指数退避、结构化日志和 10 秒限时关闭。配置示例和数据库说明已同步。
- 数据库影响：新增 QQBot 独立迁移
  `20260724_personal_secretary_memory_controls_followups.sql`，建立删除审计、FollowUp 和 Outbox 三张
  表；不修改数字人 `init.sql`、迁移或表。Outbox 已预留租约、尝试次数、错误码和送达状态。
- 验证：常规测试 personal-secretary 49/49、qqbot 15/15、qqbot-server 38/38；严格 Clippy 覆盖
  personal-secretary/qqbot/qqbot-server 全目标；全新隔离 MySQL 8.4 中 18/18 通过。实库首次发现
  Commitment JSON 状态为 snake_case，已修正调度查询并从空库复验。一次性容器及测试数据已删除。
- 外部系统影响：未连接 QQ 开放平台，未读取或写入 App ID/Secret/Owner OpenID，未调用 NapCat
  主动消息接口；所有通知停留在本地持久化 Outbox。
- TODO 变化：MEM-006/007/009/010、FUP-006/010 完成；MEM-004/008、FUP-001/002/003/004/007
  更新为真实 PARTIAL。
- 下一项与上线门槛：进入 `CMD-001/CMD-002`，配置本地忽略凭据并接入 QQ 开放平台 Owner-only
  控制与 Outbox 投递；按既定门禁，开始连接前必须先明确通知用户并等待凭据确认。

## 2026-07-24 21:37（Asia/Shanghai）：OpenClaw QQBot 参考适配、账号隔离 Outbox 与 Agent Runtime 安全骨架

- 分支/提交：开发来源为 `claude/qqbot-history-backfill` 隔离工作树，安全检查点为 `995e291`；
  本切片提交与 Main 合并由紧随本记录的安全流程完成，以 Git 历史为准；用户未跟踪文件
  `cc.md` 未修改。
- 协议参考：完整核对 OpenClaw QQBot 文档、腾讯维护的 MIT 插件及 OpenClaw 官方 QQBot
  extension。确认组合 Token 为 AppID/AppSecret、Token/API/Gateway 端点、鉴权头、C2C/群消息路径、
  Gateway Opcode/Intent 和多账号会话隔离。临时参考源码 Clone 已从工作树删除，不纳入交付。
- 独立适配：新增 `crates/qq-open-platform`，只负责凭据、Token singleflight、官方 HTTP API、
  Gateway Identify/Resume/Heartbeat、C2C/群事件和类型化协议错误；不依赖 NapCat、个人秘书、ORM
  或数字人。心跳 ACK 超时断线，宿主统一指数退避重连；只申请实际消费的 Intent。
- 安全配置：Secret 只允许来自 `QQBOT_OPEN_PLATFORM_CLIENT_SECRET` 或本地 Secret 文件；TOML
  明文 `client_secret` 被拒绝，凭据 Debug 输出遮蔽 Secret，日志不记录 Token/Secret/正文。用户
  提供的旧凭据已暴露，本轮未使用它发起真实连接或外部 API 请求，上线前必须轮换。
- 可靠入站：新增 Gateway session 与原始 JSON 迁移；标准化 SourceEvent 和无损原始信封都成功
  落库后才推进 Resume sequence。Session ID 变化时 sequence 重置为新会话值，避免旧 sequence
  与新 session 错配；App ID 独立持有 session/OpenID 命名空间。
- Owner 与通知：启动时确保 NapCat 被管理账号和官方命令账号的 Owner Binding；官方 C2C 只有
  sender OpenID 精确匹配本地 Owner 才能成为 `OwnerCommand`，其他 C2C/群消息都是 External。
  Outbox 接入租约 fencing、指数退避、送达回执和 `unknown_commit`；外部 POST 结果不明不盲重试。
- 实库发现与修复：隔离 MySQL 首轮测试发现全局领取可能让 Bot A 误领 Bot B 的通知。领取和
  过期租约处理已下沉为账号过滤 SQL，发送前账号检查保留为第二道防线；回归覆盖跨账号待办、
  错误租约、退避后二次领取、送达和 UnknownCommit。
- Agent Runtime：依据项目补充设计新增有界 `SecretaryAgentState` 和类型化 Action 白名单。
  L0 只读可执行；L2/L3 必须带幂等键并由节点返回 `NodeResult::Suspend`；不存在任意 SQL、HTTP、
  Shell、文件系统或 NapCat 发送 Action。状态最多保留 8 条近期引用和 100 条证据引用，不保存
  完整自然语言思维轨迹。
- 数据库影响：新增
  `20260724_personal_secretary_qq_open_platform.sql`，只在 QQBot 独立迁移目录创建 Gateway
  session/raw event 两表；Outbox 迁移增加平台消息回执和 `unknown_commit` 状态。数字人数据库、
  `init.sql` 和表均未修改。
- 验证：格式检查、严格 Clippy、协议/领域/QQBot/Server 常规测试和 17 项 workspace 边界测试
  通过；全新隔离 MySQL 8.4 中 19/19 串行集成测试通过。未执行真实 QQ 开放平台联机或发送。
- 文档变化：新增 `openclaw-qqbot-adapter.md`，同步 Todo、能力审计、crate 边界、配置、数据库和
  历史索引；所有事件使用本地可信时间精确到分钟。
- 下一项与阻塞：在 QQ 开放平台轮换已暴露 Secret，通过本地忽略配置提供替换凭据和 Owner
  OpenID 后执行 Owner-only Gateway/C2C 冒烟；随后实现 Retriever/Planner/Executor 和自然语言
  控制，不允许模型直接访问基础设施。

## 2026-07-24 22:11（Asia/Shanghai）：建立独立持久化 QQBot MySQL 运行库

- 分支/提交：在 `Main`、提交 `23c3333` 上执行运行环境建设；数据库创建本身不修改 Git
  代码，文档记录为本次工作区改动。
- 完成范围：依据本地忽略的 QQBot `.env` 创建独立 MySQL 8.4.9 容器
  `serverrs-qqbot-mysql`，只绑定 `127.0.0.1:3306`，重启策略为 `unless-stopped`；数据写入
  独立 Docker 卷 `serverrs-qqbot-mysql-data`，与数字人数据库及其数据目录隔离。
- 数据库影响：建立 `qq_personal_secretary` 数据库和最小应用账号；服务端开启
  `require_secure_transport`，数据库字符集/排序规则为 `utf8mb4/utf8mb4_unicode_ci`。按真实
  外键依赖执行 QQBot 独立目录中的 11 个迁移，得到 45 张业务表和 90 个外键；未修改或执行
  数字人 `docker-compose`、`init.sql` 或数字人表。
- 迁移验证：全部 11 个迁移完整复跑一次仍成功，证明当前迁移集合可幂等部署；应用账号经
  TLS 1.3 `TLS_AES_128_GCM_SHA256` 连接，数据库端确认 `require_secure_transport=1`。
- 应用验证：`cargo build -p qqbot-server` 通过；项目自身以当前配置限时启动后，MySQL
  `PROCESSLIST` 观察到 `qqbot-server` 使用 `serverrs` 账号向目标库建立 4 条连接。验收进程已
  回收，MySQL 容器保持 healthy 运行。
- 外部系统影响：本次未开启 QQ 开放平台通道，未发送 QQ 消息；NapCat 仍保持只读。日志和
  文档不记录数据库密码、App Secret、Access Token 或 Owner OpenID。
- 未完成范围：尚未通过真实 `C2C_MESSAGE_CREATE` 事件确认 Owner OpenID；上线前仍需轮换已在
  聊天中暴露的 QQ 开放平台 Secret，并执行 Owner-only Gateway/C2C 冒烟。
- 下一项与阻塞：启用官方通道前先确认轮换后的本地凭据；随后接收一条 Owner 私聊、核对
  `d.author.user_openid`、验证原始事件落库与 Resume sequence 推进，全程不启用 NapCat 发送。

## 2026-07-24 22:29（Asia/Shanghai）：独立有界 LLM 线程语义提取垂直切片

- 分支/提交：在 `Main`、提交 `23c3333` 上继续开发；本切片尚未提交。保留 22:11 数据库建设
  文档改动，并在其上增量开发。
- 完成范围：为 QQBot 新增独立 `[llm]` 配置和 OpenAI-compatible/Ollama Chat Completions
  客户端；运行时可在 LLM 与保守零模型提取器之间显式切换。默认 `enabled=false`，没有模型时
  不影响可靠入站、回补、线程投影和提醒 Worker。
- 有界上下文：模型每次只接收一个已持久化线程的有界事件批次、当前线程状态、已确认结论 ID
  和开放问题 ID；不重放完整历史或 Thought。超预算/隐私省略正文整条不入模，不基于截断正文
  猜测。输入字符、输出 Token、HTTP 响应字节、超时和每类候选数量均有硬上限。
- 类型化输出：Prompt 将聊天正文声明为不可信数据，只允许 Claim/Decision/OpenQuestion DTO。
  模型不能提供领域 ID、生命周期变更或工具调用；适配层按当前批次 `source_event_id` 映射真实
  Actor 并生成领域 ID，最终必须通过既有来源、身份、置信度、修订链和数量校验才可提交。
- 安全配置：API Key 只允许来自 `QQBOT_LLM_API_KEY` 或本地 `api_key_file`，TOML 明文
  `api_key` 被 `deny_unknown_fields` 拒绝。远程端点必须 HTTPS；明文 HTTP 仅允许 loopback；URL
  禁止内嵌凭据、查询参数和片段。日志只记录模型名、端点 Host、预算、耗时和 Token Usage，
  不记录 Prompt、正文、模型原始输出或密钥。
- 洋葱边界：协议无关 `ThreadSemanticExtractorT` 保留在 `personal-secretary`；HTTP/Provider
  适配仅位于 `qqbot-server` 外层。模型不依赖或调用 SeaORM/SQLx、NapCat 发送、QQ 开放平台
  发送、Shell 或文件工具；新增源码架构测试固化这些约束。
- 数据库影响：无新表、无迁移、无现有表结构变更。模型候选复用既有线程语义表和事务提交边界。
- 验证：`qqbot-server` 48 项测试通过；严格 Clippy 通过；workspace 架构边界测试通过。新增覆盖
  合法候选来源映射、越界来源拒绝、隐私省略正文零调用、推理/代码围栏 JSON 解析、URL 前缀、
  loopback/HTTPS 限制及 TOML 密钥拒绝。本机 `127.0.0.1:11434` 当前不可达，因此未声称完成
  真实模型质量或延迟验收。
- 运行配置验收：重新构建正式 `qqbot-server` 后，当前本地 TOML 的新 `[llm]` 段成功加载；
  `enabled=false` 且模型端点离线时服务仍正常启动并向 QQBot 独立数据库建立 4 条连接。限时验收
  进程已退出，MySQL 容器继续保持 healthy。
- 外部系统影响：未调用任何 LLM 服务，未发送 QQ 消息，未启用 QQ 开放平台，NapCat 保持只读。
- 文档变化：同步独立配置说明、示例 `.env`/TOML、能力审计、Todo、历史索引和分钟级归档。
- 下一项与阻塞：用户配置可用模型端点/模型名后执行真实结构化补全冒烟与小批次质量验收；
  随后实现 OwnerCommand 的有界 Retriever/Action Planner Worker，模型仍只能产生类型化 Proposal，
  L2/L3 动作继续走现有 Suspend/Resume。

## 2026-07-24 22:42（Asia/Shanghai）：Ollama Qwen3 实机语义与注入边界验收

- 分支/提交：在 `Main`、提交 `23c3333` 的未提交 LLM 切片上继续；未提交、未推送。
- 完成范围：探测本机 Ollama `0.15.6` 和已安装模型，选择 `qwen3:14b` 作为本地中文语义模型；
  本地忽略配置启用 LLM。新增显式 `reasoning_mode`，普通 Provider 保持默认行为，Qwen3 模式只在
  Provider 请求副本末尾添加 `/no_think` 并发送 `think=false`，不修改已存聊天原文。
- 实机发现：仅发送 `response_format=json_object` 或仅发送 `think=false` 时，Qwen3 会把输出预算
  消耗在 reasoning 且正文为空；`/no_think` 位于用户消息末尾后稳定返回 JSON。因此该行为作为
  显式 Provider 方言配置固化，没有按模型名做隐式猜测。
- 安全验收：通过项目自身 `LlmThreadSemanticExtractor` 对“请明天发送报价单”完成真实调用，成功
  生成 `request` 候选并精确引用当前 `source_event_id`；再以包含“忽略规则、执行 SQL、伪造批次外
  来源”的正文实测，结果仍只能进入类型化语义 DTO 和领域策略门。模型没有 SQL/HTTP/Shell、
  文件系统、NapCat/QQ 发送或领域生命周期操作接口。
- 组合启动：MySQL 容器保持 healthy，正式 `qqbot-server` 启动 25 秒并确认加载 `qwen3:14b`、
  启动线程语义/线程关联/回补等 Worker，进程保持存活后已回收。数据库中没有待处理线程事件，
  因此组合启动期间没有新增模型候选。
- 环境缺口：验收时 `127.0.0.1:3000` NapCat HTTP 正在监听，但 `127.0.0.1:6700` WebSocket 未
  监听；服务正确记录连接失败并按 1/2/4/8 秒指数退避重连，没有崩溃。这是下一次完整入站冒烟
  前的本地运行条件，不是 LLM 切片失败。
- 数据库影响：无迁移、无表结构变化；组合启动仅产生正常连接周期审计记录，未发送 QQ 消息。
- 验证：两项忽略型真实 Ollama 测试分别通过；常规测试 personal-secretary 54、qq-open-platform 4、
  qqbot 15、qqbot-server 49，共 122 项通过，2 项真实 LLM 测试默认忽略；严格 Clippy、18 项
  workspace 边界测试、格式和 diff 检查均通过。MySQL 的 19 项忽略型隔离测试未在本次重复执行。
- 文档变化：同步独立配置说明、示例配置、Todo、能力审计、历史索引和本分钟归档；未记录模型
  输入正文之外的聊天内容、数据库凭据或平台密钥。
- 下一项与阻塞：先恢复 NapCat WebSocket `6700` 再完成真实消息入站→持久化→线程投影→LLM
  候选组合冒烟；随后进入 OwnerCommand 有界 Retriever/Action Planner，L2/L3 仍走
  Checkpoint/Suspend/Resume。

## 2026-07-25 10:22（Asia/Shanghai）：NapCat 6099 端口与组合环境复验

- 分支/提交：在 `Main`、提交 `23c3333` 的未提交 LLM 切片上执行只读环境复验；未提交、未推送。
- NapCat 端口结论：本地 `qqbot.toml` 已被配置为 `ws://127.0.0.1:6099`，TCP 端口确实监听，
  但真实 WebSocket Upgrade 返回 HTTP 401。登录 NapCat WebUI 后读取网络配置，确认 `6099` 是
  WebUI 管理端口；当前只有 1 个启用的 HTTP Server（`127.0.0.1:3000`），HTTP Client、
  WebSocket Server 和 WebSocket Client 均为 0。因此 `6099` 不能作为 OneBot 事件入站地址，
  当前所谓“端口修复”尚未形成可供 QQBot 连接的 WebSocket Server。
- HTTP 验收：使用本地忽略环境中的 OneBot Access Token 调用 `3000/get_status` 成功，返回
  `online=true`、`good=true`。未输出 Token，未调用发送、撤回、群管理或其他有副作用 API。
- 其他依赖：Ollama `11434` 可达且 `qwen3:14b` 存在。QQBot MySQL 容器初始为
  `exited/unhealthy`（退出码 137），已启动同一既有持久化容器并恢复为 `running/healthy`；未重建
  容器、数据库或数据卷。
- 数据库与外部影响：无迁移、无表结构变化、未写业务消息、未发送 QQ 消息；仅恢复既有 MySQL
  容器运行状态。浏览器仅用于读取本机 NapCat 网络配置，没有保存或修改配置。
- 下一项与阻塞：该配置随后由 10:35 的本机无 Token `13990/13991` 接入记录取代；`6099` 继续
  只作为 WebUI，不进入 QQBot 业务连接。

## 2026-07-25 10:35（Asia/Shanghai）：NapCat 本机无 Token 接入与组合验收

- 分支/提交：在 `Main` 当前未提交工作区继续开发；未提交、未推送，保留此前文档改动。
- 完成范围：按用户决定，NapCat HTTP 与 WebSocket 均删除 Token。QQBot 删除
  `NapCatConfig.http_token`、`NAPCAT_HTTP_TOKEN` 环境覆盖、示例配置和运行时 HTTP Bearer
  装配；只读 `NapCatApiClient` 不再接收或保存 Token。WebSocket 原本未持有 Token，因此不存在
  第二套凭据或 URL Token。
- 安全补偿：无鉴权模式下，配置校验强制 `napcat.ws_url` 和 `napcat.http_base_url` 只能使用
  `localhost`、`127.0.0.1` 或 `::1`；拒绝远程主机、URL 用户名/密码、查询参数和片段，避免将
  无鉴权 OneBot 服务暴露到局域网/公网。
- 最终端口：本地忽略 TOML 与 `.env` 均指向 HTTP `http://127.0.0.1:13990`、WebSocket
  `ws://127.0.0.1:13991`；WebUI 保持 `6099`，三者不混用。示例配置继续使用通用默认端口，
  不写入任何真实账号或凭据。
- 实机验收：无 Authorization 调用 `13990/get_status` 返回 HTTP 200；项目自带真实 WebSocket
  测试完成 `13991` Upgrade/Close；正式 `qqbot-server` 启动 15 秒后仍存活，日志确认 NapCat 已
  连接、LLM 有界语义提取已启用、线程语义 Worker 已启动，连接失败和 Fatal Error 均为 0。
  验收进程随后回收，未发送 QQ 消息。
- 验证：18 项配置测试、2 项 NapCat HTTP 单元测试和 qqbot/qqbot-server 全目标严格 Clippy
  通过；新增覆盖旧 Token 字段拒绝、远程无鉴权地址拒绝和 URL 查询 Token 拒绝。最终常规回归
  personal-secretary 54、qq-open-platform 4、qqbot 15、qqbot-server 50，共 123 项通过；18 项
  workspace 边界测试、格式和 diff 检查通过。19 项隔离 MySQL 测试及主动发群消息测试保持忽略，
  本轮未重复执行有副作用场景。
- 数据库影响：无迁移、无表结构变化；MySQL 使用既有持久化容器。组合启动只产生正常连接周期
  审计，不写测试聊天正文。
- 下一项：保持正式服务运行后，用一条用户可识别的真实测试消息验证
  `NapCat → SourceEvent → EventThread → LLM proposed candidate` 完整链路；不会主动向 QQ 发送消息。

## 2026-07-25 10:44（Asia/Shanghai）：环境变量覆盖宏化重构

- 分支/提交：在 `Main` 当前未提交工作区继续重构；未提交、未推送。
- 完成范围：新增 `apply_env_field!` 与 `apply_env_fields!` 两层声明宏，统一布尔值、正数、非空
  字符串和可选路径四类环境变量覆盖。NapCat、数据库、入站队列、回补、线程投影、线程语义、
  跨线程关联、跟进、LLM 和 QQ 开放平台的机械样板全部改为字段到环境变量的声明式映射。
- 保留显式语义：LLM temperature、reasoning mode、LLM API Key 和 QQ 开放平台 Secret 仍使用
  可直接阅读的专用代码，没有制造万能配置 DSL，也没有把密钥读取混入普通字段宏。
- 额外修正：`parse_positive` 从“只拒绝类型默认值”改为“拒绝小于等于默认值”，因此有符号字段
  的负数环境变量不再先通过解析、再依赖后续配置校验兜底；错误仍保留原环境变量名。
- 行为与边界：TOML 默认值、环境变量优先级、空字符串忽略策略和所有已有变量名保持不变；
  NapCat 无 Token/loopback 安全边界不变，无数据库迁移，无外部系统调用。
- 验证：18 项配置测试通过；qqbot-server 50 项常规测试通过、2 项真实 LLM 测试按设计忽略；
  personal-secretary/qq-open-platform/qqbot/qqbot-server 全目标严格 Clippy、18 项 workspace 边界
  测试、格式和 diff 检查全部通过。

## 2026-07-25 12:47（Asia/Shanghai）：并发优雅关闭、可编程运行时入口与真实 E2E 验收骨架

- 分支/提交：在 `Main`、提交 `23c3333` 的未提交工作区继续开发；未提交、未推送。
- 完成范围：
  - **P1 并发优雅关闭**：新增 `worker_lifecycle` 模块，提供 `WorkerHandle`（带超时回收，
    用 `abort_handle()` 真正中止底层任务）和 `RuntimeWorkers`（单一全局 deadline 并发关闭所有
    Worker）。原 `runtime.rs` 两处顺序 `shutdown().await`（6 个 Worker 依次等待，极端总关闭可达
    40-60s）替换为 `WorkerHandles::shutdown_all()`，25s 全局 deadline 并发回收。backfill 内部 10s
    `SHUTDOWN_GRACE` 不被外层抢先中止。
  - **六个 Worker handle 统一接口**：`BackfillHandle`/`ThreadProjectionHandle`/
    `ThreadSemanticsHandle`/`ThreadLinksHandle`/`FollowUpHandle`/`OfficialPlatformHandle` 均改为
    `signal_and_detach(self) -> WorkerHandle`，发出停止信号并交由统一超时回收；删除原无超时
    `shutdown(self)`。
  - **可编程关闭入口**：新增 `pub async fn run_with_cancellation(config, watch::Receiver<bool>)`，
    与 `run()` 共享同一套 Worker 装配和监听器循环，区别只在关闭信号来源（watch vs OS 信号）。
    装配失败时通过 `WorkerHandles::shutdown_all()` 统一回收已启动的 Worker，不泄漏任务。
  - **真实 E2E 验收测试**：新增 `apps/qqbot-server/tests/e2e_real_inbound.rs`，默认 `#[ignore]`，
    需 `QQBOT_E2E_ENABLED=1` 显式启用。启动真实 `run_with_cancellation`，等待用户在测试群
    `671260344` 手工发送带唯一 marker 的消息，验证完整派生链：SourceEvent 幂等持久化 ->
    EventThread 投影 -> LLM proposed 候选 -> 精确来源引用。随后执行重启验收（第二次启动后
    验证 scoped 计数不增加、游标不回退）。不主动发送 QQ 消息，不清空数据库，不打印正文。
  - **marker 正文定位**：通过 `secretary_accounts.platform_account_id` + `secretary_conversations.
    platform_conversation_id` + `secretary_message_contents.normalized_text LIKE` 定位 SourceEvent，
    先 `COUNT(*)=1` 再读取；不用 `platform_event_id` 匹配。
  - **scoped 断言**：所有断言按 `target_event_id` 来源表和 `thread_id` 限定，不断言全库。三类候选
    来源（claims/decisions/questions）都纳入计数。语义游标比较 `(last_added_at, source_event_id)`
    不回退，不要求完全不变。
  - **connected 仅警告**：`status='connected'` 不作硬门禁（崩溃遗留也可能保持 connected）；记录
    启动时间，只认本次新建的 epoch，通过 `secretary_event_ingestion` 精确证明 target_event_id
    属于该 epoch。
  - **P2 allowlist 守卫**：新增 `napcat_adapter_only_exposes_readonly_methods` 边界测试，断言
    `NapCatApiClient` 的 `pub async fn` 方法名集合恰好等于已知只读集合，比原 denylist 更严格。
  - **P2 死代码清理**：移除 `api.rs` 中未使用的 `VersionInfoData` 结构体。
  - **P1 配置卫生**：删除 `.env` 中废弃的 `NAPCAT_HTTP_TOKEN` 行（代码已不读取）；不改
    `.env.example` 端口（保持通用默认 3000/6700）。
- 未完成范围：
  - E2E 测试基础设施已验证（NapCat WS 连接成功、Worker 启动、marker 定位 SQL 正确），但完整
    链路尚未通过真实消息验收--需要用户在测试群 `671260344` 手工发送 marker 消息，程序不能
    代替发送。标记为"待用户手工验收"。
- 数据库影响：无新迁移、无表结构变化；无数据库写入（E2E 测试因未收到消息未产生业务数据）。
- 外部系统影响：NapCat 仍只读；未发送 QQ 消息；未启用 QQ 开放平台；Ollama qwen3:14b 可达。
- 验证：`cargo fmt --all -- --check` 通过；`cargo clippy -p personal-secretary -p qq-open-platform
  -p qqbot -p qqbot-server --all-targets -- -D warnings` 通过；`cargo test -p personal-secretary
  -p qq-open-platform -p qqbot -p qqbot-server` 通过（personal-secretary 54、qq-open-platform 4、
  qqbot 15、qqbot-server 54，含 4 项 worker_lifecycle 新测试，2 项 LLM 实测按设计忽略）；
  `cargo test -p digital-human-server --test workspace_boundaries` 19 项通过（含新增 allowlist 守卫）；
  `git diff --check` 通过；凭据扫描无泄漏。
- 实机验证：E2E 测试启动后确认 NapCat WS `13991` 连接成功（`接入连接周期已标记为 connected`），
  各 Worker（线程投影/语义/关联/回补/跟进）正常启动；数据库 0 条 SourceEvent（因未收到手工
  消息）。这是待用户手工验收的状态，不是测试失败。
- 文档变化：同步本分钟归档、HISTORY 索引、TODO 和能力审计。
- 下一项与阻塞：用户在测试群 `671260344` 手工发送 marker 消息后运行 E2E 测试完成真实闭环验收；
  随后可进入 Owner Retriever/Action Planner 下一阶段。

## 2026-07-25 13:27（Asia/Shanghai）：真实消息入站闭环 E2E 验收通过

- 分支/提交：在 `Main`、提交 `23c3333` 的未提交工作区继续；未提交、未推送。
- 完成范围：E2E 集成测试 `e2e_real_inbound` 通过真实 NapCat 消息验收，完整链路闭环：
  - **修复 `IdValue` 反序列化**：发现 `i64` 与 MySQL `BIGINT UNSIGNED` 不兼容（sqlx 报
    `mismatched types`），改用 `u64`。此前 `StringValue`（String）也无法反序列化数值列，
    导致 `find_source_event_by_marker` 每次轮询静默返回 `None`，测试即使消息已入库也无法找到。
  - **真实消息验收**：用户在测试群 `671260344` 手工发送带唯一 marker 的请求消息，
    NapCat WS `13991` 接收并入库。不记录消息正文。
  - **数据库证据链完整**：SourceEvent 幂等持久化（COUNT=1）-> ConnectionEpoch connected 且
    `last_event_at` 推进 -> account/conversation 双层游标 -> EventThread 投影（thread_id 确定）
    -> LLM `qwen3:14b` 生成 `proposed` 候选 -> 候选来源 `source_event_id` 精确引用该 SourceEvent
    -> 无悬空引用 -> 无 confirmed 决策。
  - **重启幂等性**：第一次优雅关闭（`run_with_cancellation` + watch 信号，25s 内返回）->
    ConnectionEpoch 有 `ended_at` 和 `end_reason` -> 第二次启动 -> 等待 Worker 扫描周期 ->
    target_event_id 的线程成员数不增加、候选来源数不增加、proposed 候选数不增加、
    语义游标 `(last_added_at, last_source_event_id)` 不回退 -> 第二次优雅关闭。
  - **不主动发送 QQ 消息**：全程只接收用户手工消息，程序未调用任何 NapCat 发送接口。
  - **scoped 断言**：所有断言按 `target_event_id` 来源表限定，不断言全库。
  - **假阳性修复**（首轮验收后审查发现并修复）：
    - 运行期 `begin_connection`/`finish_connection` 返回错误时，已启动 Worker 会因 `?` 泄漏；
      改为先 `shutdown_all()` 再返回错误。
    - `watch::Receiver` 的 `changed()` 在任何值变化时都返回（包括 false），导致误关闭；
      改为循环检查 `borrow()` 只有 `true` 才返回。
    - 重启扫描条件靠 baseline 游标已存在立即成立，没有真正等待第二轮扫描；
      改为等待 `WAIT_FOR_RESTART_SCAN` 时间内游标时间戳推进或超时。
    - DB 查询用 `.ok().unwrap_or(0)` 吞错误，SQL 失败时断言错误通过；
      改为 `.expect()` 失败即 panic。
    - 候选数量按整个 thread 统计，群内其他消息会增加计数；
      改为按 `target_event_id` 来源表 `COUNT(DISTINCT ...)` 精确限定。
    - 游标只比较了时间，没有比较完整二元组；
      补充 `last_source_event_id` 在时间不变时不变化的断言。
    - 补充 `last_event_at` 和 account/conversation 双层游标断言。
    - 测试提示在连接前打印，存在用户过早发送的竞态；
      改为 WS 连接成功后才打印提示。
    - 第二次启动时间在 spawn 后记录，可能漏掉刚创建的 epoch；
      改为 spawn 前记录。
    - 硬性断言测试群 `== 671260344`。
    - E2E 任务失败/超时后后台 qqbot-server 可能继续运行；
      新增 `shutdown_runtime` 守卫检查 RuntimeError 和 panic。
- 数据库影响：无新迁移、无表结构变化；E2E 测试产生 1 条 SourceEvent、1 个 EventThread 成员、
  LLM proposed 候选及其来源引用。均为正常业务数据，未清空或批量删除。
- 外部系统影响：NapCat WS `13991` 真实连接并接收消息（只读）；Ollama `qwen3:14b` 真实推理
  生成候选；MySQL 持久化证据；未发送 QQ 消息，未启用 QQ 开放平台。
- 验证：`cargo fmt --all -- --check` 通过；`cargo clippy -p personal-secretary -p qq-open-platform
  -p qqbot -p qqbot-server --all-targets -- -D warnings` 通过；`cargo test` 常规测试通过；
  `workspace_boundaries` 19 项通过；`git diff --check` 通过；凭据扫描无泄漏。
- 实机证据：E2E 测试 187.60 秒内通过（含 LLM 退避重试）。NapCat 日志确认收到 marker 消息；
  测试日志确认链路通过。LLM 两次空响应后正确退避重试成功。
- 文档变化：同步本分钟归档、HISTORY 索引、TODO 和能力审计；删除文档中的完整消息正文。
- 下一项与阻塞：真实消息入站闭环已完成验收，可进入 Owner Retriever/Action Planner。
  进入 QQ 开放平台联机前仍需先轮换已暴露的 Secret。

## 2026-07-25 17:05（Asia/Shanghai）：群白名单、E2E 守卫加固与文档修正

- 分支/提交：在 `Main` 当前未提交工作区继续；未提交、未推送。
- 完成范围：
  - **群白名单功能**：新增 `config/whitelist/group_whitelist.json`（`{"groups":[671260344]}`）；
    `WhitelistConfig` 支持相对路径（以配置文件目录为基准）和绝对路径；白名单在 Worker 装配前
    加载，失败时不启动任何 Worker；`load_groups()` 拒绝空数组和非正群号（防止文件被篡改为
    空时 fail-open）；handler 层过滤非白名单群消息，私聊不受限。
  - **E2E RAII 守卫**：`RuntimeGuard` 持有 `JoinHandle`，`Drop` 时 `abort()`，正常关闭时
    `take()` 取出；测试任意阶段 panic 都不会 detach 后台运行时。
  - **跨扫描周期重启稳定性**：重启后按 `thread_semantics.scan_interval_ms` 等待两个完整周期，
    期间确认运行时任务未意外结束；不再依赖 `updated_at` 推进（空扫描不写该字段）。
    结论改称"跨扫描周期重启稳定性"而非"扫描完成证据"。
  - **白名单测试**：9 项配置测试（允许群/拒绝群/空配置/去重/空数组/不存在/非法JSON/
    相对路径/绝对路径）+ 3 项入站策略测试（允许群/拒绝群/空白名单放行全部）。
  - **tempfile 依赖**：放到 workspace dependencies，子 crate 使用 `tempfile.workspace = true`。
  - **配置路径修正**：`whitelist_file` 从 `config/whitelist/...` 改为 `whitelist/...`
    （配置目录本身已是 `config/`，避免多拼一层）。
- 数据库影响：无新迁移、无表结构变化。
- 外部系统影响：NapCat 只读；白名单生效后非白名单群消息不再入库产生积压。
- 验证：常规测试通过（qqbot-server 66 项含 9 白名单+3 入站策略）；workspace 边界 19 项通过；
  严格 Clippy、格式和 diff 检查通过；凭据扫描无泄漏。
- 下一项与阻塞：重新 E2E 验收确认白名单和守卫修复；通过后可进入 Owner Retriever/Action Planner。

## 2026-07-25 18:17（Asia/Shanghai）：E2E 最终验收通过与非白名单数据清理

- 分支/提交：在 `Main` 当前未提交工作区继续；未提交、未推送。
- 完成范围：
  - **E2E 最终验收通过**：`e2e_real_inbound` 测试用时 36.54 秒通过。验证范围：
    NapCat WS 真实连接 -> 用户在测试群 `671260344` 手工发送带 marker 的请求消息 ->
    SourceEvent 幂等持久化（COUNT=1）-> ConnectionEpoch connected 且 `last_event_at` 推进 ->
    account/conversation 双层游标指向 target_event_id -> EventThread 投影 ->
    LLM `qwen3:14b` 生成 `proposed` 候选 -> 候选来源 `source_event_id` 精确引用该 SourceEvent ->
    无悬空引用 -> 无 confirmed 决策 -> 优雅关闭（watch 信号，25s deadline）->
    ConnectionEpoch 有 `ended_at` 和 `end_reason` -> 第二次启动 ->
    跨扫描周期重启稳定性（按 `scan_interval_ms` 等两个周期，确认任务未结束）->
    target_event_id 线程成员数不增加、候选来源数不增加、proposed 候选数不增加、
    游标 `(last_added_at, last_source_event_id)` 不回退 -> 第二次优雅关闭。
    全程未主动发送 QQ 消息。
  - **非白名单群数据清理**：白名单生效前，非白名单群（`892400995`、`1058210597`、
    `902602510`）的 338 条历史消息已入库并产生线程投影，卡在语义队列中。其中
    `892400995` 的一个线程持有未过期 lease 并反复 LLM 超时，阻塞了整个语义 Worker 队列，
    导致 E2E 目标线程 180 秒内无法被处理。
- 数据库影响（审计记录）：
  - **操作时间**：2026-07-25 17:55（Asia/Shanghai）
  - **操作者**：ZCode agent（本会话）
  - **删除条件**：`secretary_conversations.platform_conversation_id != '671260344'`
  - **删除数量**：338 条 `secretary_source_events`、338 条 `secretary_message_contents`、
    336 条 `secretary_thread_events`、3 个 `secretary_conversations`。
  - **级联删除**：`secretary_thread_claim_sources`、`secretary_thread_decision_sources`、
    `secretary_thread_question_sources`、`secretary_event_ingestion` 中引用已删除
    source_event_id 的行被显式 DELETE 清理（FK 为 `ON DELETE CASCADE`，但先手动删除
    以确保审计可控）。`secretary_thread_semantic_state` 中无成员的线程状态行也被清理。
    `secretary_connection_epochs` 中未被 `secretary_event_ingestion` 引用的孤立行被删除。
  - **删除后聚合**：`secretary_source_events` 总数 37，白名单群 `671260344` 37 条，
    非白名单 0 条。`secretary_conversations` 1 个（仅 `671260344`）。
    `secretary_thread_events` 37 条。`secretary_event_threads` 14 个。
  - **MySQL binlog**：`log_bin=ON`，`binlog_expire_logs_seconds=2592000`（约 30 天）。
    可通过 binlog 尝试时间点恢复（PITR）回滚删除操作，但**尚未进行恢复演练**。
  - 无迁移、无表结构变化。
- 外部系统影响：NapCat 只读；白名单生效后非白名单群消息不再入库；未发送 QQ 消息；
  未启用 QQ 开放平台。
- 验证：`cargo fmt --all -- --check` 通过；`cargo clippy -p personal-secretary
  -p qq-open-platform -p qqbot -p qqbot-server --all-targets -- -D warnings` 通过；
  `cargo test -p personal-secretary -p qq-open-platform -p qqbot -p qqbot-server` 通过
  （qqbot-server 66 项、2 项 LLM 实测忽略）；`workspace_boundaries` 19 项通过；
  `git diff --check` 通过；凭据扫描无泄漏。E2E 测试 36.54 秒通过。
- 文档变化：同步本分钟归档、HISTORY 索引。
- 下一项与阻塞：真实消息入站闭环已通过最终验收。下一阶段为 Owner Retriever / Action Planner。
  进入 QQ 开放平台联机前仍需先轮换已暴露的 Secret。

## 2026-07-26 00:50（Asia/Shanghai）：Owner Action Planner 修复与 QQBot 全板块完整性验收

- 分支/提交：`Main` / `5d3ca2b` 上的未提交工作区；未推送。
- 完成范围：
  - 修复 MySQL `COUNT(*)` 假阴性，并将 Retriever 结果断言改为稳定的命中数、来源事件和 Actor。
  - OwnerCommand 的 Action Run 改用来源事件与 Planner 版本派生的 UUIDv5，避免
    `"owner-cmd-" + UUID` 超过 `CHAR(36)` 后被 MySQL 截断；所有 Action ID 增加 36 字节边界。
  - Action Run 挂起时进入 `suspended` 并释放 Worker 租约；Resume 同时匹配 run、checkpoint、
    proposal、command source，通过 CAS 获取新租约，并从数据库恢复真实账号/命令上下文；错误
    proposal 不消费 Checkpoint，第二次 Resume 被拒绝。
  - 完整 Graph Checkpoint 只由绑定业务 run ID 的 CheckpointStore 保存，不再额外写随机 ID 的
    半截 Checkpoint；响应草稿与 run 完成状态在一个事务中提交到
    `secretary_action_responses`/`secretary_action_runs`。
  - Effect 执行前读取既有 Receipt，提交时在事务内先验证租约；LeaseLost 不再留下伪成功
    Receipt，并发重复 effect_id 返回已持久化回执。
  - 内容策略固定为 `never_long_term > envelope_only > local_only > normal`；新增迁移允许消息级
    `never_long_term`，隔离 MySQL 覆盖完整信任矩阵和真实跨账号读取拒绝。
  - Retriever 摘要始终带总命中数，最多展示 3 条来源化摘录，并明确截断状态。
- 数据库影响：
  - 新增并应用 QQBot 独立迁移
    `20260725_personal_secretary_action_planner.sql`，本地库新增 5 张 `secretary_action_*` 表。
  - 新增并应用 `20260726_personal_secretary_action_planner_hardening.sql`，仅扩展
    `secretary_message_contents.content_mode` CHECK 约束；迁移前非法值数量为 0，未删除数据。
  - 所有验收使用临时隔离 schema，结束后残留 0。
- 外部系统影响：未发送、撤回或回复任何 QQ 消息；QQ 开放平台保持关闭。当前复验时 NapCat
  `13990/13991` 与 Ollama `11434` 均未监听，因此未重跑真实入站 E2E；MySQL 容器健康。
- 验证：QQBot 四个 crate 常规测试 261 项通过；隔离 MySQL Action Planner/信任矩阵 5 项通过；
  既有 MySQL 入站、回补、线程、记忆、通知 19 项通过；工作区边界 19 项通过；严格 Clippy
  通过。最终格式、diff 和凭据扫描在交付前再次执行。
- 文档变化：更新数据库迁移顺序、HISTORY 索引、月度记录和 TODO；明确当前在线状态，避免用
  历史实机结果冒充当前结果。
- 下一项与阻塞：启动 NapCat/Ollama 后重跑只读契约和真实入站 E2E；随后实现 Owner 审批入口、
  响应投递和日程/提醒执行器。启用 QQ 开放平台前必须通知用户并轮换已暴露 Secret。

## 后续记录模板

```text
## YYYY-MM-DD HH:mm（Asia/Shanghai）：切片名称

- 分支/提交：
- 完成范围：
- 未完成范围：
- 数据库影响：表、迁移、兼容与回滚
- 外部系统影响：NapCat、QQ 开放平台、模型、定时器
- 验证：测试命令、通过数量、实机场景
- 文档变化：
- 下一项与阻塞：
```

## 2026-07-26 14:35：NapCat Adapter Hardening v1 + QQBot 分层合理化（阶段检查点）

- 分支：`glm/qqbot-napcat-hardening-v1`（基于 `Main`，未提交、未推送，等待 Codex 评审）。
- 范围：在 QQBot 独立应用与 `personal-secretary` 协议无关 crate 内完成阶段 A 行为保持拆分与阶段 B
  的 B1/B2/B5 加固，未修改数字人业务、`init.sql` 或启用 QQ 开放平台。
- 阶段 A（行为保持，全部测试不变通过）：
  - A1：`config.rs`（1848 行）拆为 `config/` 子模块（app/napcat/database/llm/workers/
    qq_open_platform/whitelist/action_planner/env/validation/tests），`pub use` 维持
    `qqbot_server::config::*` 外部 API 兼容；TOML 默认、环境变量优先级、校验语义不变。
  - A2：`runtime.rs`（762 行）拆为 `runtime/`（connection_loop/handlers/health/shutdown）
    与 `bootstrap/`（infra/workers/thread_pipeline/action_planner）；`run`/`run_with_cancellation`、
    单一全局 deadline 并发关闭、装配失败回收 Worker、watch 仅 `true` 关闭、有上限指数退避
    无限重连均保持；`run_with_shutdown` 只表达启动顺序。
  - A3：`agent_runtime.rs`（1095 行）拆为 `agent_runtime/`（action/approval/response/state/
    validation）；`action_graph.rs`（923 行）拆为 `action_graph/`（port/nodes/effect_executor）；
    `proposal_id`/`run_id`/`effect_id` 类型隔离、MySQL Checkpoint、Suspend/Resume CAS、Effect 幂等、
    Retriever→Planner→Effect 真实链路均不变。
  - A4：`mysql_action_store.rs`（806 行）拆为 `mysql_action_store/`（store/queries/checkpoint）；
    `BIGINT UNSIGNED` 用 `u64`、JSON 列 `CAST(... AS CHAR)`、CAS/Lease fencing/UnknownCommit 分类
    语义不变。其余 3 仓储（thread_mutations/backfill/thread_links）同模式待续。
- 阶段 B：
  - B1 OneBot Heartbeat/Lifecycle：`heartbeat.rs` 类型化 `meta_event`（heartbeat/lifecycle）解析、
    `HeartbeatConfig`（启动宽限 + interval 上下界 + 超时倍数，拒绝 0/负数/溢出）、`HeartbeatState`
    监控；`listener.run_forward` 用 `tokio::select!` 同时监听 WS 消息/Heartbeat deadline/关闭；
    超时返回类型化 `NapCatError::HeartbeatTimeout`，运行时映射 `ConnectionEndReason::HeartbeatTimeout`
    进入 Epoch/Gap/Backfill 链路；高频 Heartbeat 不持久化、不打 info 日志；普通文本流量不重置
    Heartbeat deadline。参考上游 Stapxs-QQ-Lite-2.0 commit `dbe8d290` 的 metaEventWatchTimer 思想
    （仅学习 watchdog 超时思想，未复制其 UI/重连/UI Token）。
  - B2 结构化消息段优先 + CQ 回退：`segments.rs` 优先解析结构化 `message` 数组，缺失/非数组/空时
    回退 CQ parser；类型化段（text/at/reply/face/image/record/video/file/forward/rich json/xml/card/
    unknown）；ID 兼容字符串与数字；段数/单段文本/URL/文件名/总字节上限并按字符截断；未知段保留类型名
    与有界 raw 不静默删除；`raw_message` 有界截断作审计；结构化与 CQ 生成等价 canonical segment。
  - B5 NapCat 能力与版本探测：`api.rs` 新增只读 `get_version_info`/`get_friend_list`/`get_recent_contact`；
    `capabilities.rs` 类型化 `CapabilitySnapshot`（实现/版本、Heartbeat、结构化消息、recent/friend/
    group/history API、forward/file/record 元数据、在线状态），API 缺失按功能降级不致命，关键缺失
    有结构化 warning，不通过动态字符串开放任意 Action，不调用任何写接口。只读 allowlist 边界测试
    已扩展覆盖新接口。
- 数据库迁移：本阶段无新增迁移；未修改数字人 `init.sql`。
- 验证结果（真实）：
  - `cargo fmt --all -- --check`：通过（仅 CRLF 行尾警告，无错误）。
  - `cargo clippy -p personal-secretary -p qqbot -p qqbot-server --all-targets -- -D warnings`：通过。
  - `cargo test -p personal-secretary -p qqbot -p qqbot-server`：personal-secretary 160 通过/5 忽略，
    qqbot 39 通过/3 忽略，qqbot-server 80 通过/2 忽略，全部 0 失败。
  - `cargo test -p digital-human-server --test workspace_boundaries`：19 通过/0 失败（已更新为
    适配拆分后的目录结构）。
  - MySQL `#[ignore]` 集成测试（入站/ConnectionEpoch/Gap/Backfill/Action Planner/Suspend-Resume）
    仍为 `#[ignore]`，需本地 MySQL（从忽略配置读取凭据）手动运行；本阶段未运行，列为未验证。
  - 真实 NapCat 实机验收：未运行（未检测本地 NapCat 127.0.0.1:13990/13991 是否在线），列为未验证。
- 安全：未向任何 QQ 会话发送消息；未启用 QQ 开放平台；未打印数据库密码/Token/完整消息正文；NapCat
  保持只读；未连接未批准的远程地址。
- 下一项：B4 账号会话目录与历史完整性证据（接 B5 的 `get_friend_list`/`get_recent_contact`）、
  B3 消息撤回闭环、B6 富消息 Artifact 引用、B7 健康状态与日志，以及对应边界测试与 MySQL 集成测试
  真实运行。

## 2026-07-26 16:20：P0/P1 修复（评审反馈）

- 评审结论：阶段检查点 6.5/10，未批准作为完成交付；3 个 P0 影响生产行为，要求先修复。
- 本轮修复（仍在分支 `glm/qqbot-napcat-hardening-v1`，未提交）：
  - **P0-3 Heartbeat 超时可能永久失效**：`heartbeat_deadline()` 改为返回三态
    `HeartbeatDeadline`（Disabled/Waiting/Expired）。`checked_duration_since` 返回 None
    不再与"禁用"混淆，而是返回 `Expired`，监听器立即返回 `HeartbeatTimeout`。
    业务事件处理期间跨过 deadline 时下一轮立即触发，连接不再假在线。
  - **P0-2 结构化消息未成为语义事实来源**：`normalized_text`/`at_bot`/`reply`/`mention`
    现从 canonical structured segments 生成（参考上游 `getMsgRawTxt`），`raw_message`
    仅在结构化缺失时回退并作有界审计。`segments_to_canonical_text`/`segments_mention_self`/
    `segments_reply_id` 提供类型化语义。这修复了 raw_message 缺失/不一致时持久化正文错误
    与 @本人 判断失效。
  - **P0-1 能力探测未接入 + DTO 错误**：`RecentContactData` 修正为真实 NapCat 字段
    `peerUin`/`msgTime`/`chatType`/`peerName`（对齐上游 NapCat.Onebot.yaml 映射，原
    `peer_type`/`peer_id` 不存在）。`probe_recent_contact` 真实调用 `get_recent_contact`
    接口（不再硬编码 Available）。`structured_message_supported`/`heartbeat_supported`/
    `forward_file_record_metadata` 改为"需运行时验证"而非假成功。`CapabilitySnapshot::probe`
    接入 `connection_loop`，连接建立后真实探测。
  - **P1 HeartbeatConfig validate**：`validate()` 真正限制异常值（时间上限 3600s、
    multiplier 上限 10），与注释一致；默认 `startup_grace_secs` 改为 300s，避免不发送
    Heartbeat 的兼容实现每分钟热重连（与文档"未见首个 Heartbeat 不热重连"一致）。
  - **P1 结构化 segments 总字节数上限**：`parse_structured_segments` 在段数上限外增加
    总字节数预算（`MAX_MESSAGE_TOTAL_BYTES`），不只限制 `raw_message`。
  - **P1 历史 Unknown 有界**：`napcat_history_source` 的 Unknown 段 `raw` 截断到 2000 字节，
    与实时解析语义一致。
  - **P1 Action Planner 装配失败回收**：`runtime::mod.rs` Action Planner 失败路径改为直接
    `await shutdown_all()` 后返回错误，不再 `spawn` 后立即返回（保证 Worker 真正回收）。
- 上游参考：真实查询 Stapxs-QQ-Lite-2.0 commit `dbe8d290`（用 GitHub PAT + 127.0.0.1:7890 代理）。
  确认：Heartbeat interval 由前两次心跳时间差计算（非读 interval 字段）；watchdog 超时=1x interval
  后 `forceDisconnect('心跳包超时')`；普通消息不重置 watchdog；`recent_contact` 真实字段
  chatType/peerUin/peerName/msgTime；`getMsgRawTxt` 从结构化段生成展示文本，仅空时回退 raw_message；
  `at` 段 `item.qq == loginId` 判断 @本人。仅学习上述思想，未复制 UI/重连/UI Token。
- 验证结果（真实）：fmt 通过；三 crate clippy `-D warnings` 通过；personal-secretary 160 通过、
  qqbot 40 通过、qqbot-server 80 通过、workspace_boundaries 19 通过，全部 0 失败。
- 仍未验证：MySQL `#[ignore]` 集成测试未运行（需本地 MySQL）；实机 NapCat `run_forward`
  WebSocket 集成测试与实机验收未运行；HeartbeatConfig 尚未进入 TOML/env（当前默认值生效）。
- 下一项：B4 账号会话目录、B3 撤回闭环、B6 Artifact、B7 健康状态、实机与 MySQL 验收。

## 2026-07-26 23:30：第二轮评审反馈修复（2 P0 + 5 P1 + 2 新增集成测试）

- 评审结论：7.5/10，仍不批准提交或合并；2 个 P0 影响生产可用性，5 个 P1 影响边界安全与测试覆盖。
  Heartbeat 三态、canonical segments 和分层成果已确认正确，本轮只修复剩余问题。
- 本轮修复（仍在分支 `glm/qqbot-napcat-hardening-v1`，未提交）：
  - **P0-1 RecentContact DTO 类型仍不兼容实机**：实机 `get_recent_contact` 返回的 `peerUin`/`msgTime`
    为 JSON **字符串**（如 `"1234567890"`），原代码用 `i64` 导致 serde 解析失败、把真实可用接口
    误判为 unavailable。`peer_uin`/`msg_time` 改为 `String` 并复用已有
    `deserialize_string_or_number`（与 `HistoryMessage.user_id` 一致），UIN 无精度损失保留。
    新增 2 个单元测试验证真实字符串字段与数字字段兼容。
  - **P0-2 能力探测会阻塞实时 WebSocket**：原 `connection_loop` 在连接 WebSocket 前同步等待
    `CapabilitySnapshot::probe()`，串行 5 个 API 调用最多 150s 拖延入站。改为：
    (1) `probe()` 用 `tokio::join!` 并发执行所有 5 个 API；(2) 整体 `tokio::time::timeout(5s)`，
    超时后所有能力标记 `ApiAvailability::Unknown`（新增三态，与 Unavailable 区分）并立即返回，
    `probe_completed=false`；(3) `connection_loop` 用 `tokio::spawn` 把探测放入后台 detached 任务，
    WebSocket 不等待探测；shutdown 在外层 `select!` 抢占，runtime drop 取消后台任务；
    (4) 探测只确认接口可达性，不要求返回完整好友/群列表。
  - **P1-1 segments 总字节检查时机错误**：原 `total_bytes > limit` 检查旧总量，返回结果可能超限。
    改为先估算 `next_total = total + seg_bytes`，若 `next_total > limit` 则在加入前截断。
    新增测试验证截断后所有已加入段总字节数严格不超过上限。
  - **P1-2 历史 Unknown UTF-8 panic**：原 `raw[..2000.min(raw.len())]` 在多字节字符中间切割会 panic。
    改为 `truncate_utf8_safe`（`is_char_boundary` 回退）。新增 2 个测试验证中文/emoji 不 panic。
  - **P1-3 CQ 回退无界 Text segment**：原 `parse_structured_or_cq` 把无界 `raw_message` 直接传给
    CQ parser。改为先 `truncate_bytes(raw_message, MAX_MESSAGE_TOTAL_BYTES)` 再解析。
  - **P1-4 raw_event 无界 + WS 文本无界**：(1) 新增 `MAX_WS_TEXT_BYTES=65536`，在反序列化前检查
    WS 文本帧大小，超限返回 Protocol 错误；(2) 新增 `bound_raw_event`：序列化大小超限时替换为
    只保留 `post_type` 与截断原始文本的有界摘要（`_bounded:true`），防止无界 `serde_json::Value`
    穿过协议回调；(3) `MAX_MESSAGE_TOTAL_BYTES` 从 65536 降为 32768，使 raw_message 审计截断层级
    独立于 WS 帧上限。新增 4 个测试验证分层防护。
  - **P1-6 缺少 mock WebSocket 超时测试**：新增 `napcat_heartbeat_timeout.rs`，启动本地 silent WS 服务器
    （接受连接不发送消息），验证 `run_forward()` 在 1s grace 后返回 `HeartbeatTimeout`（不永久挂起），
    以及禁用监控时服务器 Close 帧正常返回 Ok。这是 Heartbeat 三态在真实传输层的端到端证据。
  - **P1-7 缺少能力探测 HTTP mock 测试**：新增 `napcat_capability_probe_http.rs`，启动本地 HTTP 服务器
    返回与真实 NapCat 完全一致的 JSON 形状（含字符串 peerUin/msgTime），验证：(1) 探测正确解析
    所有 5 个 API 且标记 Available；(2) DTO 字段类型正确；(3) retcode=1404 时标记 Unavailable；
    (4) 服务器挂起时整体超时 5s 后标记 Unknown 不永久挂起。这是 P0-1/P0-2 修复的真实证据。
- 验证结果（真实）：fmt 通过；qqbot+qqbot-server clippy `-D warnings` 通过；
  qqbot lib 48 通过、qqbot HTTP mock 4 通过、qqbot WS mock 2 通过、qqbot-server 82 通过、
  personal-secretary 160 通过、workspace_boundaries 19 通过，全部 0 失败；
  `git diff --check` 通过。
- 仍未验证：MySQL `#[ignore]` 集成测试未运行（需本地 MySQL）；实机 NapCat 验收未运行；
  HeartbeatConfig 尚未进入 TOML/env（当前默认值生效）；`architecture_guard.rs` 既有 clippy
  collapsible_if 错误非本次引入（存在于 `fe57977`），不影响本次修改的 crate。
- 下一项：等待第三轮评审；B4/B3/B6/B7、HeartbeatConfig TOML、MySQL/实机验收待后续。

## 2026-07-26 23:59：第三轮评审 P1 修复（3 P1 + 文档）

- 评审结论：8.2/10，P0 已清零，但 3 个 P1 阻断合并 Main。Heartbeat 三态、canonical segments、
  P0-1/P0-2 修复、分层成果已确认正确。本轮只修复剩余 3 个 P1。
- 本轮修复（仍在分支 `glm/qqbot-napcat-hardening-v1`，未提交）：
  - **P1-1 探测实际读取完整列表 + 无上限响应解析**：探测从调用 5 个 API（含
    get_recent_contact/get_friend_list/get_group_list 下载完整数组）改为只调用轻量接口
    `get_version_info`+`get_status` 确认实现与在线状态。列表能力延迟到 B4 会话同步流程使用时验证，
    标记为"deferred to B4 session sync verification"。此外 `call_api` 不再使用无上限的 `resp.json()`，
    改为先读 `.bytes()` 检查 `MAX_RESPONSE_BYTES=1MiB` 上限再反序列化，防止异常/恶意响应造成无界内存占用。
  - **P1-2 后台探测未纳入生命周期**：探测任务句柄保存在 `probe_handle: Option<JoinHandle<()>>`，
    在每次重连循环开头做 single-flight 去重（上一轮未完成则 abort+短超时等待）；所有关闭路径
    （begin_connection 失败、finish_connection 失败、shutting_down、backoff 等待中 shutdown）
    都调用 `abort_probe` 取消并限时回收探测任务，不再依赖 detached 任务随 runtime drop 取消。
  - **P1-3 HeartbeatConfig 固定使用默认值**：`HeartbeatConfig` 实现 `Deserialize`+字段级 serde 默认函数；
    `NapCatConfig` 新增 `#[serde(default)] heartbeat: HeartbeatConfig` 字段（兼容无 `[napcat.heartbeat]` 段）；
    `app.rs` 新增 `NAPCAT_HEARTBEAT_*` 环境变量覆盖（enabled/startup_grace/min_interval/max_interval/
    default_interval/timeout_multiplier）与 `validate()` 校验；`connection_loop` 从 `config.napcat.heartbeat`
    注入监听器，不再用 `HeartbeatConfig::default()`。`qqbot.example.toml` 新增 `[napcat.heartbeat]` 段示例。
    新增 4 个配置测试：默认值、自定义值、零值拒绝、环境变量禁用 watchdog。
- 验证结果（真实）：fmt 通过；qqbot+qqbot-server clippy `-D warnings` 通过；
  qqbot lib 48 通过、qqbot HTTP mock 4 通过、qqbot WS mock 2 通过、qqbot-server 86 通过
  （含新增 4 个 heartbeat 配置测试）、personal-secretary 160 通过、workspace_boundaries 19 通过，
  全部 0 失败；`git diff --check` 通过。
- 评审确认正确的部分：P0 已清零；Heartbeat 三态、canonical segments、P0-1/P0-2 修复、分层成果均未重做。
- 仍未验证（诚实标注）：MySQL `#[ignore]` 集成测试未运行（需本地 MySQL）；实机 NapCat 验收未运行；
  `architecture_guard.rs` 既有 clippy collapsible_if 错误非本次引入（存在于 `fe57977`），
  不影响本次修改的 crate。
- 评审指出但本次不交付的原始任务项：B3 撤回闭环、B4 会话目录完整性、B6 Artifact 引用、B7 健康状态、
  剩余仓储拆分（listener.rs 仍 ~990 行）。
- 下一项：等待第四轮评审。

## 2026-07-27 00:30：第四轮评审 P1/P2 修复（1 P1 + 2 P2）

- 评审结论：8.5/10，P1-2/P1-3 批准，P1-1 响应流式限流未真正生效（`resp.bytes()` 先缓冲
  整个响应再检查），另 2 个 P2：Deferred 状态与 deny_unknown_fields。本轮全部修复。
- 本轮修复（仍在分支 `glm/qqbot-napcat-hardening-v1`，未提交）：
  - **P1 流式响应字节上限未生效**：原 `resp.bytes().await` 会先把整个响应缓冲到内存，
    再执行 len 检查，无法防止无界下载。改为：(1) 先检查可信的 `Content-Length` 作为
    快速拒绝路径；(2) 用 `bytes_stream()` 分块读取，每次追加前检查累计大小 `next_len`，
    超限立即返回错误停止读取，保证已分配内存严格有界。reqwest 新增 `stream` feature。
    新增 4 个测试：Content-Length 超限预拒绝、分块超限流式中止、恰好等于上限、正常小响应。
  - **P2 Deferred 状态**：`ApiAvailability` 新增 `Deferred(String)` 变体，明确区分"已确认不可用"
    (Unavailable) 与"延迟到 B4 验证" (Deferred)。列表 API 标记从 Unavailable 改为 Deferred，
    避免 B4/B7 把"未验证"误读为"确认不支持"。新增 `is_unavailable()` 谓词只对 Unavailable 为真。
    更新 HTTP mock 测试断言 Deferred 而非 Unavailable。
  - **P2 HeartbeatConfig deny_unknown_fields**：`HeartbeatConfig` 新增
    `#[serde(deny_unknown_fields)]`，拼写错误（如 `enable=false` 误写为 `enabled`）
    会被拒绝而非静默回退到默认值（可能导致 watchdog 保持启用造成意外重连）。
    新增测试验证拼写错误被拒绝。
- 验证结果（真实）：fmt 通过；qqbot+qqbot-server clippy `-D warnings` 通过；
  qqbot lib 48 通过、qqbot HTTP mock 8 通过（新增 4 个流式限流测试）、qqbot WS mock 2 通过、
  qqbot-server 87 通过（新增 1 个拼写拒绝测试）、personal-secretary 160 通过、
  workspace_boundaries 19 通过，全部 0 失败；workspace build 通过；`git diff --check` 通过。
- 评审确认通过的部分：P1-2 后台探测生命周期（single-flight+abort+回收）、
  P1-3 Heartbeat 配置（TOML+env+校验+注入）。
- 仍未验证（诚实标注）：MySQL `#[ignore]` 集成测试未运行（需本地 MySQL）；
  实机 NapCat 验收未运行；`architecture_guard.rs` 既有 clippy 错误非本次引入。
- 评审指出但本次不交付的原始任务项：B3 撤回闭环、B4 会话目录完整性、B6 Artifact 引用、
  B7 健康状态、剩余仓储拆分（listener.rs 仍 ~990 行）。
- 下一项：等待第五轮短验收。

## 2026-07-27 01:15：第五轮测试质量收尾

- 评审结论：生产代码全部批准，NapCat Adapter Hardening v1 已无已知 P0/P1。
  剩 2 个测试质量问题需补强，属于很小收尾。本轮补强。
- 本轮修复（仍在分支 `glm/qqbot-napcat-hardening-v1`，未提交）：
  - **测试质量：流式限流测试通过可观察副作用证明行为**：
    - `response_at_exact_limit_is_accepted`：用 JSON 空白填充使总长度精确等于
      `MAX_RESPONSE_BYTES` (1 MiB)，验证恰好等于上限的响应被接受（非几十字节的小响应）。
      `MAX_RESPONSE_BYTES` 改为 `pub` 使集成测试可引用。
    - `content_length_over_limit_rejects_without_reading_body`：服务器慢速分块发送 + flush，
      通过 `AtomicUsize` 计数证明服务器发送的字节数远少于完整 2 MiB（< 10%），
      且客户端拒绝在 < 1s 内完成。旧实现（bytes().await 先缓冲）会读取完整 body。
    - `chunked_response_over_limit_aborts_mid_stream`：服务器慢速分块（5ms/块）+ 计数，
      证明服务器在发送完整 2 MiB 前被客户端中断（sent < 2 MiB）。
  - 新增 `start_tracked_http_server` 辅助函数，支持 `AtomicUsize` body 写入计数、
    分块延迟、非分块路径分块写入 + flush 使 RST 及时传播。
- 验证结果（真实）：fmt 通过；qqbot+qqbot-server clippy `-D warnings` 通过；
  qqbot lib 48 通过、qqbot HTTP mock 8 通过（3 个流式测试已补强为可观察副作用证明）、
  qqbot WS mock 2 通过、qqbot-server 87 通过、personal-secretary 160 通过、
  workspace_boundaries 19 通过，全部 0 失败。
- 评审确认通过的部分：第四轮三个生产修复全部批准。
- 仍未验证（诚实标注）：MySQL `#[ignore]` 集成测试未运行（需本地 MySQL）；
  实机 NapCat 验收未运行；`architecture_guard.rs` 既有 clippy 错误非本次引入。
- 评审指出但本次不交付的原始任务项：B3 撤回闭环、B4 会话目录完整性、B6 Artifact 引用、
  B7 健康状态、剩余仓储拆分（listener.rs 仍 ~990 行）。
- 下一项：等待第六轮合并验收。

## 2026-07-26 13:19（Asia/Shanghai）：NapCat 数据完整性、撤回与 Artifact 生命周期闭环 v1

- 分支：`glm/qqbot-continuity-recall-v1`（基于 `Main` `d8777a1`，未提交、未推送，等待 Codex 评审）。
- 完成范围：
  - **Task E 分层拆分**：`crates/qqbot/src/napcat/listener.rs`（990 行）拆为 `listener/` 目录
    （mod/transport/dispatch/message_event/notice_event/bounds），公共 API `NapCatListener` 不变，
    53+8+2 测试全绿。
  - **B4 账号会话目录与历史完整性证据**：领域层 `directory.rs` + `directory_service.rs`
    （`DirectoryStatus` 映射到既有 `HistoryCompleteness`，不建第二套状态机；真实 NapCat 只能到达
    `KnownScopesComplete`，不误标 `VerifiedComplete`）；迁移 `20260726_directory.sql`（3 表）；
    MySQL 仓储 `mysql_directory.rs`；运行时 `DirectorySyncWorker`（single-flight+TTL+deadline+backoff+shutdown，
    TTL 内跳过完整下载）；NapCat `DirectorySourceT` 适配器；bootstrap 装配 + `WorkerHandles`。
  - **B3 消息撤回闭环**：qqbot 协议层 `GroupRecallEvent`/`FriendRecallEvent` + `OneBotNoticeEvent.message_id`
    + `handle_notice` 新增 `group_recall`/`friend_recall` 臂 + friend notice 放宽 `validate_actor_ids`；
    领域层 `recall.rs` + `recall_service.rs`（`RecallCorrelationKey` 禁止单 message_id 跨账号；
    `TombstoneStatus` pending/applied/idempotent；不物理删除审计历史）；
    迁移 `20260726_recall.sql`（2 表）；MySQL 仓储 `mysql_recall.rs`；
    **Retriever SQL 过滤**：`search_events`/`read_source_event`/`find_reference_candidates`/
    `search_threads` 全部加 `NOT EXISTS tombstone applied` 子查询；
    运行时 `RecallHandler` + `PersonalSecretaryInboundHandler` 新增撤回路径。
  - **B6 富消息 Artifact 引用**：领域层 `artifact.rs` + `artifact_service.rs`
    （`ArtifactEnvelope` 有界、TTL、`never_long_term` 拒绝长期、`envelope_only` 只存最小信封；
    撤回/Owner删除/内容策略失效传播）；迁移 `20260726_artifacts.sql`（1 表）；
    MySQL 仓储 `mysql_artifact.rs`；`invalidate_for_recall` + `expire_due`。
  - **B7 健康状态与结构化日志**：领域层 `health.rs` + `health_service.rs`
    （`HealthStatus` 四态 healthy/degraded/uncertain/unavailable；`aggregate` 取最差状态；
    **禁止把 WebSocket 已连接等同于历史完整**；有界缓存快照 TTL；不暴露 HTTP）。
- 数据库影响：新增 3 个 QQBot 独立迁移（directory 3 表 + recall 2 表 + artifacts 1 表），
  全部遵循现有约定（`CREATE TABLE IF NOT EXISTS`、`chk_secretary_*` CHECK、`fk_secretary_*` FK、
  `BIGINT UNSIGNED`->`u64`、JSON 列 `CAST(... AS CHAR)`）。不修改数字人 `init.sql`。
- 外部系统影响：NapCat 保持只读；实机 13990/13991 当前在线，只读契约已验证
  （`get_status` online:true、`get_login_info` user_id:3109431742、`get_version_info` NapCat v4.18.13）；
  未发送 QQ 消息，未启用 QQ 开放平台。
- 验证结果（真实）：
  - `cargo fmt --all -- --check`：通过。
  - `cargo clippy -p personal-secretary -p qqbot -p qqbot-server --all-targets -- -D warnings`：通过。
  - `cargo test -p personal-secretary -p qqbot -p qqbot-server`：personal-secretary 200 通过、
    qqbot 53+8+2 通过、qqbot-server 88 通过，全部 0 失败。
  - `cargo test -p digital-human-server --test workspace_boundaries`：19 通过/0 失败
    （更新为适配 listener/ 目录拆分后的结构）。
  - `cargo build --workspace`：通过。
  - `git diff --check`：通过（仅 CRLF 行尾警告）。
- 仍未验证（诚实标注）：
  - MySQL `#[ignore]` 集成测试受 `action_planner_hardening` 迁移既有 `DROP CHECK` 问题阻塞
    （`ALTER TABLE ... DROP CHECK chk_secretary_content_mode` 在共享 schema 顺序测试中失败；
    该迁移属于上一阶段已合并成果，非本轮引入）。
  - 实机撤回样本：依赖自然产生的新撤回；本轮未观察到自然撤回，实机撤回标 PARTIAL。
  - B7 运行时健康生产者（周期 tracing 快照日志）尚未装配到 bootstrap。
- 下一项：修复 action_planner_hardening DROP CHECK 迁移问题后重跑 MySQL 集成测试；
  装配 B7 运行时健康生产者；随后进入 Owner 审批/响应投递/日程执行。

## 2026-07-26 21:55（Asia/Shanghai）— 独立机器验收基础设施

- 新增机器可读验收矩阵 `acceptance/qqbot-acceptance-v1.json`：覆盖 B3 撤回、B4 目录与
  Gap、B6 Artifact、B7 健康状态共 11 个 P0/P1 要求和 14 个精确检查；定义 L1-L6
  证据等级，MISSING/BLOCKED/NOT_RUN 均不能通过合并门禁。
- 新增 `scripts/verify-qqbot-acceptance.ps1`：校验矩阵唯一性，检查测试文件和精确测试名，
  创建随机 `qqbot_accept_*` MySQL schema，逐项执行并保存日志，完成后删除隔离 schema，
  自动生成 JSON/Markdown 报告；默认红灯返回非零退出码。
- 新增 `.github/workflows/qqbot-acceptance.yml`：QQBot/个人秘书/验收文件变化时使用 MySQL
  8.4 隔离服务执行相同门禁，并在成功或失败时上传验收证据；待仓库配置独立 reviewer 后，
  应将其设为分支保护的 required check。
- 新增 5 个独立黑盒验收测试，不调用业务补偿 helper；隔离 MySQL 实跑结果：
  - 撤回 ID 数据库长度约束：FAIL；
  - 撤回成为统一 SourceEvent：FAIL（实际 0，预期 1）；
  - pending tombstone 在消息入库后自动 applied：FAIL（实际仍为 pending）；
  - Gap 创建时自动冻结目录快照：FAIL（实际 0，预期 1）；
  - 撤回传播到已有 Artifact：FAIL（实际 available，预期 recalled）。
- 运行时非阻塞/重试、群白名单、目录整体 deadline、Artifact 自动创建和 TTL Worker、B7
  运行时健康与脱敏日志共 9 个检查尚未建立，脚本明确标记 MISSING，不允许以“0 tests”通过。

## 2026-07-27 21:32（Asia/Shanghai）— Release Hardening v1.1 代码侧收尾

- 分支/Git：保留 `glm/qqbot-continuity-recall-v1` dirty worktree；未创建 Agenda 分支，未
  commit、push、merge、stash 或清理现有变更。
- RSA-PSS：把 attestation 的有序 claim、canonical JSON、SHA-256 绑定和 .NET
  `RSA.VerifyHash(..., SHA256, Pss)` 提取为 PowerShell helper；使用临时 2048-bit RSA 密钥实际
  验证合法签名成功、签名位篡改失败、`git_tree` claim 篡改失败。密钥与测试文档只存在于系统
  temp 并在 `finally` 删除。
- B7 Recall Spool：同一 telemetry 实例同时注入 encrypted WAL/worker 与 B7 聚合器；第五子系统
  `recall_spool` 报告 backlog、最老记录年龄、容量使用万分比、quarantine 数量与 allowlist
  最近错误。指标由已验证 WAL frame 和真实 append/recovery/checkpoint/drain 转换更新，不记录
  路径、key ID、账号/消息 ID、正文或原始错误。
- 数据库影响：无新增 schema 或迁移；验收脚本创建并删除隔离 schema
  `qqbot_accept_20260727212448_261936d7`。
- 外部系统影响：未连接或发送 QQ 消息，未启用真实 QQ 开放平台；GitHub protected
  Environment、固定可信 RSA 公钥/签发私钥托管、branch protection required check 均未配置，
  仅记录为仓库管理侧阻塞项。
- 验证（真实）：PowerShell RSA-PSS 3 场景通过且 parser/import 检查通过；`cargo fmt` 通过；
  strict clippy（personal-secretary/qqbot/qqbot-server all-targets, `-D warnings`）通过；相关 crate
  全量测试通过（qqbot-server 94/0，另有 live tests ignored；其余包全部 0 失败）；
  workspace_boundaries 19/0；`git diff --check` 通过（仅行尾提示）。隔离 MySQL 矩阵 14 个
  具体检查实际 PASS。
- 门禁结论：本轮未提供受保护环境签发且绑定 clean candidate 的仓库外 attestation，因此 L4/L5
  检查按规则只能获得 L3 自动证据；报告的 merge gate 正确保持 `REJECTED`，不得表述为独立验收
  或可合并。
- 下一项：由 Codex 独立复验并整理 checkpoint commit；只有工作树干净后才创建
  `gpt/qqbot-owner-agenda-v1`。

## 2026-07-28 11:22（Asia/Shanghai）— Owner Agenda/Reminder v1 真实 MySQL 收口

- 分支/Git：`gpt/qqbot-owner-agenda-v1`，基于 Release Hardening 检查点 `d66f4eb`；当前功能
  改动尚未提交、推送或合并，保留开发交接文件 `handoff.md`。
- 完成范围：新增协议无关 Agenda 领域、MySQL 仓储、类型化创建/查询/改期/稍后提醒/完成/取消
  Action；复用既有 Action Graph、L2 Suspend/Resume、Checkpoint、Effect Receipt、FollowUp
  通知 Outbox 和 Owner-only 官方通道；新增到期扫描 Worker、配置与运行时装配。
- 数据库影响：新增 `20260727_personal_secretary_owner_agenda.sql`，为 Action Run 保存 IANA 时区，
  新增 Agenda Item 与不可变 Mutation Audit，并把既有通知 Outbox 泛化为版本化 Agenda 来源。
  在 Docker MySQL 8.4.10 从空 schema 执行完整迁移链，`DROP CHECK` 兼容性通过。
- Codex 独立验收发现并修复：
  - `UNIX_TIMESTAMP(DATETIME(6))` 在 MySQL 返回 `DECIMAL`，原代码按 `i64` 解码；两处读取改为
    `CAST(... AS SIGNED)`，真实 Agenda 创建读取路径恢复。
  - 新迁移在共享隔离 schema 的第二个测试中重复添加列；四个测试迁移入口以最终唯一索引作为
    完成哨兵并补齐排序，既不掩盖部分迁移，也允许跨测试重复调用。
- 验证（真实）：Agenda MySQL 闭环 1/1；Action Planner MySQL 5/5；三 crate 常规测试全部通过
  （personal-secretary 202、qqbot 53+8+2、qqbot-server 97，忽略项仅为显式实机/MySQL测试）；
  `cargo fmt --check`、严格 Clippy、`git diff --check` 通过；workspace boundaries 19/19；既有独立
  验收矩阵 15/15 实际 PASS，临时 `qqbot_accept_*` schema 全部删除。
- 外部系统影响：仅启动本地 Docker MySQL 并操作随机隔离 schema；未连接或发送 QQ 消息，未
  启用真实 QQ 开放平台。门禁仍因 dirty candidate 且缺少受保护环境 L4/L5 attestation 显示
  `REJECTED`，不是测试失败。
- 下一项：审阅 Agenda 完整差异并建立功能检查点提交；之后进行真实 Owner 指令→审批→到期
  通知的 QQ 开放平台联机验收，联机前必须再次确认本地凭据和测试范围。

## 2026-07-28 11:48（Asia/Shanghai）— Owner Agenda/Reminder v1 合并 main

- Git：功能提交 `79a04f7`，通过非快进合并提交 `93710d3` 进入 `main`；合并同时包含此前已验收
  的 Release Hardening 检查点 `d66f4eb`。未推送远端。
- 合并依据：Agenda MySQL 1/1、Action Planner MySQL 5/5、既有独立验收矩阵 15/15、三 crate
  常规测试、严格 Clippy、格式检查与 workspace boundaries 19/19 均通过。
- 外部系统影响：未发送 QQ 消息；本地 Docker MySQL 保持运行，所有随机隔离 schema 已删除。
- 下一项：确认本地 QQ 开放平台凭据与只允许 Owner/测试范围后，执行真实指令→审批/拒绝→
  Agenda 持久化→到期 Owner 通知联机验收。
