# 个人 QQ 智能秘书开发历史：2026-07 归档

> 最后更新：2026-07-24
> 本文件只记录已经发生且有证据的事项；未来计划统一放在 `TODO.md`。

## 2026-07-23：产品方向切换为个人智能秘书

- 形成 V2.0 需求草案：NapCat 作为个人 QQ 全消息感知通道，QQ 开放平台作为 Owner 控制与
  提醒通道，知识库延后。
- 明确 NapCat 在 MVP 中只读，不代表 Owner 自动回复任何联系人或群。
- 明确采用有界结构化状态、最近窗口和按需来源检索，而不是反复重放全部历史。
- 文档状态：需求草案仍位于 Main 根目录且未跟踪，本次未移动或覆盖。

## 2026-07-23：首个统一身份和消息入口切片

- 分支/工作区：`codex/qq-personal-secretary`，隔离工作树
  `.worktrees/qq-personal-secretary`。
- 新增 `crates/personal-secretary`，定义消息来源、账号主体、会话、可信发送者、消息角色和
  幂等键。
- 指令不变量：只有 QQ 开放平台 Owner 控制会话中的已验证 Owner 是 `OwnerCommand`；
  NapCat 中 Owner 自己发送的内容是 `OwnerObservation`。
- NapCat 新增私聊和 `message_sent` 事件；群聊行为保持兼容。
- `qqbot-server` 将 NapCat 消息映射到统一身份边界，不记录消息正文，不发送消息，不写数据库。
- 数据库影响：无迁移、无表变更、无现有数据读写。
- 外部验证：本地 NapCat WebSocket 实机握手成功；未进行真实聊天正文采集。
- 自动验证：个人秘书、QQ 适配器和服务端聚焦测试 24 项通过，架构边界 9 项通过；格式化和
  差异检查通过。
- 已知基线：严格 Clippy 被 `qqbot/napcat/api.rs` 7 个既有 `needless_question_mark` 告警
  阻断；本次范围在仅豁免该规则后通过。
- Git 状态：截至本记录，改动尚未提交或合并。

## 2026-07-23：因果链、主动跟进和结构化记忆审计

- 确认 Reply ID 目前只停留在协议消息段，尚未解析为持久化事件因果边。
- 确认当前没有 SourceEvent、EventThread、Decision、OpenQuestion、Commitment、Scheduler、
  Secretary Memory 或 Owner 自然语言 Action。
- 确认 `agent-core` 的 Graph、Effect、Checkpoint、Suspend/Resume 可以复用，但尚未建立个人
  秘书业务状态和节点。
- 建立 `docs/qq-personal-secretary/` 作为后续 QQ 智能秘书项目文档的正式入口。
- 建立能力审计、分阶段 Todo 和本历史记录；下一开发切片确定为可靠事件存储。

## 2026-07-23：独立配置与可靠消息持久化切片

- QQBot 配置入口独立为 `apps/qqbot-server/config/qqbot.toml`，示例和本地 `.env` 也只位于
  该目录；仅使用 `QQBOT_CONFIG_PATH`、`QQBOT_DATABASE_URL` 和 QQBot/NapCat 专属变量。
- QQBot 数据库迁移独立位于 `apps/qqbot-server/database/migrations`；新增四张
  `secretary_*` 表，不修改数字人的 `database/sql/init.sql` 和迁移目录，不复用旧 `qq_*` 表。
- 新增协议无关的消息内容段、@成员、@全体和 Reply 建模，并将 NapCat 载荷映射到个人秘书域。
- 新增 `InboundEventStoreT` 与 SeaORM/MySQL 实现；消息按“来源账号 + 平台消息 ID”幂等落库，
  同一平台消息 ID 在不同账号主体下保持独立。
- `qqbot-server` 改为先落库：新事件才允许进入后续处理，Duplicate 不重复处理。
- Docker MySQL 8 隔离验收通过：首条插入、重复去重、Reply 因果边、@成员、@全体和跨账号
  隔离均验证成功。
- 未完成：连接周期、Cursor、Gap、历史回补、有界队列/背压、Reply 待回填和非消息事件。
- Git 状态：截至本记录，改动仍位于隔离工作树，尚未提交或合并。

## 2026-07-23：连接周期、游标与不确定空窗切片

- 新增协议无关的 `ConnectionEpochId`、连接状态/结束原因、`IngestionCursorScope` 和
  `IngestionGapStatus`；连接健康和历史完整性是两个独立状态。
- 新增独立连续性迁移，建立连接周期、实时事件来源关联、账号/会话游标和空窗四张
  `secretary_*` 表；没有修改数字人数据库或根 `init.sql`。
- NapCat WebSocket 握手成功前先创建连接周期；握手成功后持久化 `connected`，消息落库时
  在同一事务更新连接最后事件、账号游标和会话游标。
- 远端断开、传输失败和进程退出都会结束周期；曾经成功连接的周期创建
  `IngestionGap(status=uncertain)`。下次连接只写入空窗结束时间，不会把状态改成已补齐。
- QQBot 的 SeaORM 运行时启用 Rustls；隔离 MySQL 使用 `ssl-mode=required` 完成 TLS 验收。
- Docker MySQL 8 验收：两份迁移可重复执行，消息幂等/Reply/@ 测试与连接周期/双层游标/
  空窗幂等测试连续两轮通过。
- 未完成：NapCat 历史 API 契约、回补 Worker、Gap 完整性判定、有界队列/背压和本地 Spool。
- 平台门槛：进入 QQ 开放平台连接阶段前必须先通知用户并确认凭据只保存在 QQBot 本地配置。
- Git 状态：截至本记录，改动仍位于隔离工作树，尚未提交或合并。

## 2026-07-23：NapCat 历史接口只读收口与契约探测

- 对照 NapCat 官方接口文档和本机在线实例，验证群聊/私聊历史接口可返回标准消息、发送者、
  `message_id` 和 `message_seq` 字段；验证过程没有输出或保存实际聊天正文及联系人 ID。
- 新增 `get_group_msg_history`、`get_friend_msg_history` 和 `get_msg` 类型化读取方法；ID 全部
  按字符串解析，历史单页限制为 1–100。
- 从 NapCat HTTP 客户端删除个人账号发送、戳一戳等修改能力，并增加源码架构守卫。
- 发现重复探测可能返回成功空页；因此只完成读取适配器，尚未启动自动回补，也没有把任何
  `IngestionGap` 标记为 `verified_complete`。
- 新增 `napcat-history-contract.md`，记录官方契约、本机证据和锚点/分页/PacketBackend 未决项。
- QQ 开放平台仍未开始接入；到达该阶段前继续执行“先通知用户、再配置本地凭据”的门槛。

## 2026-07-23：有界入站队列、背压与可调试日志切片

- `qqbot-server` 的 NapCat 回调改为只做类型映射和非阻塞 `try_send`；MySQL 幂等写入从
  WebSocket 回调移到独立有界 `mpsc` Worker。
- Worker 对临时数据库错误执行有上限的指数退避，单条消息成功或确认为重复后再消费下一条；
  无效事件停止重试并留下可审计错误。
- 队列满时不阻塞 WebSocket，聚合丢弃计数并为当前连接周期幂等创建
  `IngestionGap(status=uncertain, reason=queue_overflow)`，不把无法证明的连续性伪装成完整。
- 连接结束时限时排空 Worker；超时会中止内存队列并依赖不确定空窗与后续历史回补。当前尚无
  本地磁盘 Spool，因此数据库长期离线叠加进程崩溃仍可能丢失尚未落库的事件。
- 新增独立 QQBot 队列容量、重试初始/上限和退出排空超时配置；不读取数字人配置。
- 新增 `trace/debug/warn/error` 结构化日志，覆盖入队、持久化尝试、幂等结果、重试、溢出 Gap
  和 Worker 排空；日志不记录聊天正文或凭据。
- 数据库影响：没有新增迁移或修改数字人表；复用 QQBot 自己的连续性表记录溢出 Gap。
- 验证：个人秘书/QQ 适配器/QQBot 服务端聚焦测试 31 项通过；隔离 MySQL 真实集成测试 2 项
  通过；跨业务架构边界 13 项通过；严格 Clippy、格式和差异检查通过。数据库重试恢复、队列满
  立即返回和容量配置上下限均有自动测试。
- QQ 开放平台仍未开始接入。

## 2026-07-23：双 NapCat 实例只读契约复验

- 两个本机 NapCat 实例均验证为不同的在线测试账号；双方互为好友，并同时属于指定测试群。
- 通过 WebUI 内部临时调试适配器对两个账号执行只读 OneBot 调用；没有发送消息、撤回、退群、
  修改网络配置或输出聊天正文及真实账号 ID。
- 双方的好友列表、群列表、群成员、群历史和私聊历史读取均成功；群历史重复读取保持稳定。
- 精确 `message_seq` 是包含式锚点；不存在的相邻序号返回失败，不能用序号加减实现可靠翻页。
- `get_msg` 在消息所属账号内往返成功，跨账号查询同一消息 ID 失败；这为“来源账号 + 平台消息
  ID”幂等键提供了实机证据。
- 新增忽略型 `napcat_live` 契约测试；第一实例的真实 OneBot HTTP 读取和 WebSocket 握手 2 项
  通过。第二实例尚未配置 OneBot HTTP/WebSocket 服务器。
- 第一实例当前关闭本人消息上报；本轮测试群/私聊历史样本较少且只有文本段，因此 @、Reply、
  本人消息、撤回、断连和多页稳定翻页仍不能标记完成。
- 数据库影响：无。QQ 开放平台仍未开始接入。

## 2026-07-23：双账号测试群主动契约与真实入库验收

- 获得仅在指定测试群发送主动测试消息的授权；测试代码没有 `send_private_msg`，没有向好友或
  其他群发消息。
- 临时为第一实例开启本人消息上报，并为第二实例增加独立 WebSocket；每次运行都先保存完整
  配置，`finally` 清理后重新读取验证，两份配置均与原快照逐字一致。
- 新增忽略型 `napcat_active_group_live` 契约测试，覆盖未 @、明确 @、本人消息、Reply、撤回
  通知、Listener 重建、双账号历史、`get_msg` 和精确历史锚点；最终 1 项实机测试通过。
- 发现并验证 NapCat 消息 ID 按观察账号分域：回复方必须使用自己收到的父消息 ID，接收方的
  `Reply` 会转换为其账号视角的 ID。双账号各 18 条历史样本的消息 ID 交集为 0。
- 撤回通知已确认存在，但当前 Listener 只记录“尚未建模”的 Debug 日志；该缺口登记为
  `EVT-008`，没有把撤回误报为已经持久化。
- 启动真实 `qqbot-server` 连接一次性 MySQL 8：8 张 QQBot 自有 `secretary_*` 表成功迁移；
  四条测试群消息全部入库，其中 Owner 1、External 3、@ 1、Reply 1，Reply 父事件解析成功，
  四条均关联连接周期，账号/会话两级游标均存在。
- 运行期使用 `trace/debug/info/warn` 结构化日志；日志未记录聊天正文、访问令牌或 WebUI 凭据。
- 最终回归：个人秘书/QQ 适配器/QQBot 服务端 31 项通过，跨业务隔离守卫 13 项通过；严格
  Clippy、格式化和差异空白检查通过。顺带等价化简 6 处只读 API 的冗余 `Ok(...?)`。
- 所有主动测试消息均通过发送账号撤回，真实服务进程已停止，NapCat 配置已恢复；一次性
  MySQL 容器和本次主动验收构建目录均已删除。
- QQ 开放平台仍未开始接入。

## 2026-07-24：历史回补生命周期与潜在故障加固

- 分支/工作区：`claude/qqbot-history-backfill`（同一工作树，在 2026-07-23 切片之上修复）。
- 背景：2026-07-23 切片被过度声明 DONE；端到端走查和系统性审计发现多项 Gap 生命周期、
  调度、租约并发、身份隔离与隐私错误路径问题，本次集中修复并补齐失败测试。
- 完成范围：
  - #1 开放空窗不可回补：`claim_next_gap` SQL 增加 `gap_ended_at IS NOT NULL`。
  - #2 uncertain Gap 可重入不饿死：移除 `secretary_backfill_runs.gap_id` 唯一键；新增
    `secretary_gap_reclaim_schedule(next_eligible_at)`；`Unprovable` 退避后可重试，
    `KnownScopesComplete` 自动挂起，只在人工重验或来源能力升级后重新排队；
    `ORDER BY updated_at ASC` 防止饿死后续 Gap。
  - #3 边界快照 + message_id 匹配：新增 `secretary_gap_boundaries`，`finish_connection`/
    `mark_connection_uncertain` 创建 Gap 时用 `INSERT...SELECT ON DUPLICATE KEY`（首写获胜）
    冻结会话游标；`known_scopes_for_gap(gap_id)` 读快照；用例边界按 message_id 匹配。
  - #4 Worker 关闭可退出：`BackfillHandle` 加取消状态，领取等待和在飞任务均监听关闭；
    关闭后有界清理并中止卡住任务。
  - #5 真实并发：引入 `BackfillRunner` trait，`scan_once` 用 `tokio::task::JoinSet` 真正
    spawn `max_concurrency` 个任务。
  - #6 租约 CAS + 配置化 + 错误分类：`reclaim_expired` 加 `FOR UPDATE`+CAS；`MySqlInboundEventStore`
    持有 `lease_secs`（`build_mysql_backfill_store(db, lease_secs)`）；`map_source_error` 不再因
    retcode=200 判 `PermissionDenied`（改 Protocol 暂时性）。
  - #7 恢复调度：把“领取过期运行”和“执行历史恢复”拆开，领取数量受并发上限约束，恢复
    进入统一 `JoinSet`；移除会误杀合法长恢复的固定 5 秒超时。
  - #8 错误退避：第一次错误使用 `retry_initial_ms`，随后指数增长并封顶；退避是下次扫描前
    不可绕过的最短等待，不再被 30 秒 ticker 或重连 wake 提前打断。
  - #9 租约 fencing：新增 `secretary_backfill_leases` 所有权表；首次领取和每次过期接管均
    轮换令牌，远程读取前续租验权，进度写入和终态提交必须匹配当前令牌；旧 Worker 迟到
    写入返回 `LeaseLost`，同令牌的已提交终态重试仍幂等成功。令牌独立建表，使重复执行迁移
    可以为已应用早期草稿 runs 表的环境补齐 fencing，不依赖不受 MySQL 支持的幂等 ADD COLUMN。
  - #10 有界恢复：崩溃恢复从持久化 Scope 进度恢复已消耗事件数，不再把
    `max_events_per_run` 预算重置为 0。
  - #11 身份与隐私：历史 Scope、响应 `self_id` 和群 `group_id` 必须一致；缺失稳定
    message ID/sequence 的页面不再静默跳过；NapCat 失败响应的完整 `data` 不再传播到日志或
    Scope 证据。
- 数据库影响：修改未合并迁移 `20260723_personal_secretary_backfill.sql`（移除唯一键、新增
  `secretary_gap_boundaries`、`secretary_gap_reclaim_schedule` 和
  `secretary_backfill_leases`）；不修改
  数字人迁移或根目录 `init.sql`。
- 验证：personal-secretary/qqbot/qqbot-server 常规测试 72 项通过；隔离 MySQL 8.4 集成测试
  12 项全通过，覆盖迁移重复执行、开放 Gap、退避/挂起、成功路径、并发过期接管、令牌轮换、
  旧令牌拒绝与终态幂等重试；一次性 MySQL 容器已停止并删除。严格 Clippy、架构边界和最终
  差异检查见本次交付前复验结果。
- 未完成：真实 NapCat 实机多页/跨重启验证仍未做（测试用 Fake 来源）；本地 Spool（GAP-007）、
  撤回投影（EVT-008）不在范围。
- Git 状态：改动仍位于隔离工作树，尚未提交或合并。

## 2026-07-23：历史回补 Worker 与 Gap 完整性证据闭环

- 分支/工作区：`claude/qqbot-history-backfill`，隔离工作树
  `.worktrees/qqbot-history-backfill`，基线 `ad343a8`。
- 完成范围：
  - 领域层（`crates/personal-secretary/src/backfill.rs`）：协议无关的回补模型
    `BackfillRunId`/`BackfillScope`/`BackfillCursor`/`BackfillAnchor`/`BackfillBudget`/
    `BackfillEvidence`/`BackfillOutcome`/`HistoryCompleteness`、Gap 状态机转换规则
    `validate_gap_transition`、完整性证据判定、预算校验。不依赖 NapCat/SeaORM/Tokio。
  - 应用层（`crates/personal-secretary/src/backfill_service.rs`）：端口
    `HistoryBackfillSourceT`/`BackfillStateStoreT`/`BackfillStateStoreWithIngestionT` 与
    `BackfillGapUseCase` 编排（领取 Gap、读已知 Scope、有界分页、统一幂等入口、保存进度
    与证据、决定 Gap 状态转换）。
  - MySQL 仓储（`crates/personal-secretary/src/infra/repo/mysql_backfill.rs`）：原子领取
    `uncertain -> backfilling` + 租约、`reclaim_expired` 崩溃恢复、`known_scopes_for`、
    `record_scope_progress`、`load_run_progress`、`finalize_run`。
  - Reply 子先父后回填（`mysql_inbound.rs`）：父消息入库时同账号批量回填未解析的
    `reply_to_event_id`，幂等且不跨账号。
  - 迁移 `apps/qqbot-server/database/migrations/20260723_personal_secretary_backfill.sql`：
    `secretary_backfill_runs`/`secretary_backfill_scopes`，可重复执行，外键/索引/回滚说明。
  - NapCat 历史适配（`apps/qqbot-server/src/backfill/napcat_history_source.rs`）：实现
    `HistoryBackfillSourceT`，复用 `NapCatInboundMapper::map_core` 使历史与实时同一套身份
    规则；锚点绑定账号视角，不跨账号、不数值加减。
  - 独立 Worker（`apps/qqbot-server/src/backfill/worker.rs`）：与实时 WebSocket 解耦，周期
    扫描 + 重连唤醒，`max_concurrency` 信号量限制，结构化日志含 run_id/gap_id/completeness/
    pages/accepted/duplicates，不记录正文/Token/完整响应。
  - 配置（`apps/qqbot-server/src/config.rs`）：`[backfill]` 段与 `QQBOT_BACKFILL_*` 环境变量，
    `page_size∈1..=100`、`max_concurrency∈1..=64`、`retry_max≥retry_initial` 等校验。
- 未完成范围：本地磁盘 Spool（GAP-007）、账号级会话集合完整性证明（真实 NapCat 不可证）、
  撤回事件投影（EVT-008）、实机多页方向与跨重启翻页稳定性。
- 数据库影响：新增前向迁移 `20260723_personal_secretary_backfill.sql`，建立两张
  `secretary_backfill_*` 表；不修改数字人库或既有 `secretary_*` 表；回滚顺序见迁移末尾。
- 外部系统影响：NapCat 仍只读；未发送/撤回/修改配置；无新主动测试授权。
- 验证：`cargo fmt --all -- --check` 通过；`cargo test -p personal-secretary -p qqbot
  -p qqbot-server` 通过（personal-secretary 26、qqbot 15、qqbot-server 20，MySQL/live
  忽略型测试未运行）；`cargo clippy -p personal-secretary -p qqbot -p qqbot-server
  --all-targets -- -D warnings` 通过；`cargo test -p digital-human-server --test
  workspace_boundaries` 16 项通过。MySQL 隔离集成测试（迁移顺序/重复、原子领取/租约恢复、
  幂等、Reply 回填、跨账号隔离、用例+真实 MySQL）为忽略型，需 `QQBOT_TEST_DATABASE_URL`。
- 文档变化：更新 TODO（GAP-004/005 DONE、EVT-007 PARTIAL 补充子先父后）、HISTORY、
  napcat-history-contract、capability-assessment、crate-boundaries、database/config README。
- 下一项与阻塞：因果线程层（THR-*）；账号级会话完整性需更多实机证据；本地 Spool 待评估。

## 2026-07-24：确定性 EventThread 批量投影切片

- 分支/工作区：`claude/qqbot-history-backfill`，继续在隔离工作树追加，未提交、未合并。
- 完成范围：
  - 领域层新增 `EventThread`、`ThreadClaim`、`ThreadDecision`、`OpenQuestion`、
    `ThreadRelation`、生命周期和来源证据类型；不依赖 NapCat、MySQL 或 Tokio。
  - `DeterministicThreadPlanner` 对有界批次执行 Reply 父链优先、同会话短窗口聚合和新线程
    兜底；发送者只形成同会话窗口内的附加关系，绝不单凭同一发送者跨群自动合并。
  - 应用层新增 `ThreadProjectionStoreT` 与 `ThreadProjectionUseCase`，一次领取、一次规划、
    一次事务提交；默认路径不调用 LLM，也不重放完整消息历史。
  - MySQL 新增线程、线程成员、确定性关系和独立投影租约四张 `secretary_*` 表；投影消费者
    不复用 `source_events.processing_status`，租约过期可接管，提交前执行令牌验权。
  - `qqbot-server` 新增独立批量 Worker、QQBot 专属配置、扫描上限、指数退避、可取消关闭和
    `trace/debug/warn` 日志；日志不记录聊天正文。
- 未完成范围：明确项目 ID/文件版本/非 Reply 引用的结构化提示、要求/反对/确认提取、结论
  修订链、线程生命周期推进、跨会话候选链接、隐私策略和人工拆分/合并仍属于 THR-002/004～010。
- 数据库影响：新增前向迁移 `20260724_personal_secretary_threads.sql`，只位于 QQBot 独立迁移
  目录，不修改数字人 `init.sql` 或数字人表；回滚顺序写在迁移末尾。
- 外部系统影响：未调用 NapCat HTTP/WS 发送能力，未发送消息，未连接 QQ 开放平台；用户报告
  NapCat `127.0.0.1:3000`/`127.0.0.1:6700` 已启动，本切片无需修改其配置。
- 验证：领域 Reply/窗口/跨群/时间戳漂移测试通过；Worker 卡住关闭测试通过；一次性 MySQL
  8.4 中 13 项集成测试串行全通过，覆盖四消息两线程、跨窗口 Reply、同群近邻、跨群隔离、
  空扫描和既有回补回归；测试容器与临时数据已删除。
- 下一项与阻塞：THR-004 类型化提取需要先定义候选补丁和来源引用，再决定是否接入 LLM；
  THR-002 的项目 ID/文件版本必须来自结构化提示，不能以同名文本直接自动合并。

## 2026-07-24：线程类型化语义、修订链与生命周期安全底座

- 完成范围：新增 `ThreadSemanticExtractorT`/`ThreadSemanticStoreT`/`ThreadSemanticUseCase`；
  提取器只返回类型化候选补丁，不能直接写库或改变线程终态。
- 当前保守提取器按线程有界批次识别明确的请求、反对、确认、决定和问句；所有候选默认
  `proposed`，模糊聊天不生成事实，“请问”不会误判为动作请求。
- 安全校验：候选必须引用本批次原始事件、参与者必须完整匹配已观察身份、置信度/文本/
  候选数/来源数均有上限；超字符预算正文整条跳过推断，不使用截断文本。
- 结论修订：新增 `supersedes_id` 唯一链，候选只能引用同线程 confirmed 结论，不允许新总结
  静默覆盖旧结论。
- 生命周期：实现 `open/waiting/resolved/closed/reopened` 合法转换和不可变来源历史；关闭必须
  有已验证 `OwnerCommand` 来源且不存在开放问题。
- 数据库影响：新增独立迁移 `20260724_personal_secretary_thread_semantics.sql`，增加语义游标/
  租约及候选、来源、修订、问题、状态历史共 9 张 `secretary_*` 表；数字人数据库不变。
- 隐私：`never_long_term` 会话及 `envelope_only` 正文不进入语义提取；完整跨会话检索授权和
  既有派生数据清理仍属于 THR-009 后续范围。
- 运行时：新增独立语义 Worker、QQBot 专属配置、扫描/字符/事件预算、错误退避、可取消关闭
  和无正文结构化日志；不调用 NapCat 发送接口，也不接入数字人 LLM。
- 验证：常规领域/服务测试、严格静态检查和架构守卫见交付结果；一次性 MySQL 8.4 中 14 项
  集成回归串行全通过，包含 26 表迁移幂等、候选/来源持久化、隐私过滤和旧回补链；容器已删除。
- 未完成：自由文本上下文/指代提取、Owner 确认/撤销控制面、自动结束条件、跨会话候选链接和
  人工拆分/合并仍待 THR-004～010 后续切片。

## 2026-07-24 14:10（Asia/Shanghai）：跨群/私聊线程关联候选闭环

- 分支/工作区：`claude/qqbot-history-backfill`，继续位于隔离工作树；本节与代码均尚未提交或
  合并，因此 Main 工作区暂时不可见。
- 完成范围：新增 `ThreadLinkSignalKind`、`ThreadLinkHint`、`ThreadLinkCandidate`、来源证据和
  候选状态；领域校验仅允许明确项目 ID、精确文件 `source_key` 两类强证据生成候选，并要求
  账号一致、线程/会话不同、线程对规范排序、理由与证据类型一致。
- 安全边界：同名人物、相似话题和相同文件名被显式建模为弱证据，不能通过领域校验；候选
  固定为 `proposed`，本切片从不修改 `secretary_thread_events`，不会自动合并线程。Owner
  查看、接受、拒绝和拆分操作仍留给后续控制面。
- 隐私：提示值仅以 SHA-256 指纹保存，不复制项目编号、文件源键或文件名明文；扫描继续排除
  `never_long_term` 会话和 `envelope_only` 正文。日志只输出批次数量、候选数和耗时，不输出
  聊天正文或提示值。
- 应用/运行时：新增有界 `ThreadLinkUseCase`、独立 MySQL 租约/幂等状态和
  `[thread_links]` Worker；支持事件/字符/批次数上限、租约过期接管、指数退避、卡住任务可取消
  关闭及 `trace/debug/warn` 调试日志。
- 数据库影响：新增 QQBot 独立迁移 `20260724_personal_secretary_thread_links.sql`，建立扫描状态、
  强提示指纹、关联候选和候选来源 4 张 `secretary_*` 表；不修改数字人 `init.sql` 或数字人表。
  回滚按来源表、候选、提示、扫描状态顺序人工执行。
- 验证：常规测试 personal-secretary 42 项、qqbot 15 项、qqbot-server 36 项通过；一次性 MySQL 8.4 隔离容器
  中 15 项集成回归串行全通过，包含跨群/私聊强证据候选、双方来源、相同文件名不关联、隐私
  会话不派生，以及全部既有回补/投影/语义测试。首次验收发现 `COUNT(*)` 有符号解码差异，已
  修正后复验；测试容器和临时数据已删除。
- 外部系统影响：只做了 `127.0.0.1:3000` HTTP 端口和 `127.0.0.1:6700` WebSocket 端口的
  TCP 可达性检查，均可连接；未调用 NapCat 主动消息接口，未发送任何 QQ 消息；未连接 QQ 开放平台，也未
  创建或读取 App ID、Secret、Owner OpenID。进入 CMD-001/CMD-002 前仍必须先明确通知用户，
  等待确认后才把凭据写入 Git 忽略的 QQBot 本地配置，绝不写入文档、日志或提交。
- TODO 变化：THR-007 标记 DONE；THR-002、THR-008、THR-009 更新为当前真实完成边界。
- 下一项与阻塞：优先补 Owner 候选查看/接受/拒绝控制入口与错误关联拆分；这将进入 CMD
  控制面和 QQ 开放平台接入门槛，需要先通知用户并等待本地凭据确认。

## 2026-07-24 14:19（Asia/Shanghai）：Owner 关联审核与跨主体授权闭环

- 完成范围：新增 `ThreadLinkReviewUseCase`，支持按账号和稳定游标分页查看关联候选、双方会话、
  有界原文片段、置信度及理由；支持接受/拒绝并返回幂等 Receipt。
- 权限模型：审核命令必须来自 QQ 开放平台的 `OwnerCommand`，并通过本地
  `secretary_owner_bindings` 显式绑定到被管理的 NapCat 账号。普通观察、未绑定 Owner、绑定到
  其他账号的命令均拒绝；授权绝不从聊天文本、昵称或相同 QQ 数字猜测。
- 并发/审计：候选行在事务内 `FOR UPDATE`，状态以 `proposed` CAS 更新；每个候选和命令事件
  最多一条不可变审核记录。相同命令重试返回原 Receipt，不同终态竞争失败。
- 非自动合并：`accept` 只把候选标记为 `accepted`，不改写 `secretary_thread_events`，跨群和
  私聊消息仍保留各自线程。显式搬移、拆分和合并继续属于 THR-010 后续高影响操作。
- 数据访问约定：普通 CRUD 后续优先使用 SeaORM Entity/Query；当前显式 SQL 仅保留在 `infra`
  适配器，并用于 `FOR UPDATE SKIP LOCKED`、租约 fencing、MySQL upsert、跨表隐私过滤和 CAS
  等 ORM 难以准确表达的并发语义，领域层与应用层不依赖 SQL。
- 数据库影响：在未合并的新迁移 `20260724_personal_secretary_thread_links.sql` 中追加
  `secretary_owner_bindings` 和 `secretary_thread_link_reviews`；不保存 App ID/Secret，不修改
  数字人数据库。
- 验证：领域/应用常规测试 personal-secretary 43 项、qqbot-server 36 项通过；隔离 MySQL 8.4
  中 15 项集成回归串行全通过，覆盖未绑定/跨账号/非命令拒绝、来源分页、接受、重复幂等、
  单条审计和不自动合并；一次性容器与数据已删除。
- 外部系统影响：未连接 QQ 开放平台，未配置或读取任何真实凭据，未调用 NapCat 主动消息接口。
- TODO 变化：THR-008、THR-010、CMD-003 更新为 PARTIAL 的真实完成边界。
- 下一步：先补稳定 CRUD 的 SeaORM Entity，并实现高影响线程拆分/合并的 Suspend/Resume 影响
  预览；真正进入 CMD-001/CMD-002 或连接 QQ 开放平台前，必须先通知用户并等待本地凭据确认。

## 2026-07-24 14:33（Asia/Shanghai）：历史分块、SeaORM CRUD 与线程变更 Suspend 预览

- 文档治理：根 `HISTORY.md` 改为当前阶段、最近事件和归档导航；既有 349 行原文迁入
  `history/2026-07.md`。归档达到 500 行或 100 KiB 后按日期/业务切片继续拆分，旧记录没有
  可信分钟时不猜测回填。
- 数据访问：新增线程关联候选、审核 SeaORM Entity；审核记录查询/插入和候选 CAS 更新迁到
  SeaORM Query/ActiveModel。`FOR UPDATE SKIP LOCKED`、Owner 绑定授权联表、指纹匹配和 MySQL
  upsert 等并发/方言语义继续封装在 `infra` 显式 SQL 中。
- 高影响动作：新增 `ThreadMutationImpact`、`ThreadMutationApprovalRequest` 和纯领域校验；
  Merge 仅允许 2～10 个不同线程，Split 只允许一个来源线程，事件/会话计数、理由和来源样本
  均有上限且禁止重复 ID。
- Agent Runtime：`suspend_thread_mutation_for_approval` 直接返回类型化
  `NodeResult::Suspend`，原因为 `Approval`；Suspend 结果不携带 Effect，因此 Owner 确认前绝不
  移动消息或写入线程成员。
- 数据库影响：没有新增迁移表；复用未合并的候选/审核表。SeaORM Entity 只映射稳定 CRUD，
  不改变现有数据结构。
- 验证：personal-secretary 45 项、qqbot-server 36 项通过；隔离 MySQL 8.4 中 15 项集成回归
  全通过，覆盖 SeaORM 审核 CRUD、Owner 绑定、幂等审核及既有回补/线程链；Agent Runtime
  64 项和架构边界 16 项通过；完整依赖严格 Clippy、格式和差异检查通过；一次性容器已删除。
- 未完成：Proposal/Checkpoint 持久化、Resume 输入验权、确认后的 Effect 执行和 UnknownCommit
  处理尚未实现，当前只完成“安全地产生影响预览并暂停”。
- 外部系统影响：未连接 QQ 开放平台，未读取或配置真实凭据，未发送 NapCat 消息。
- TODO 变化：THR-010、CMD-008 更新为 PARTIAL。

## 2026-07-24 16:24（Asia/Shanghai）：线程变更持久化 Checkpoint/Suspend/Resume 与逻辑执行闭环

- 分支/提交：`claude/qqbot-history-backfill` 隔离工作树；本次未提交、未合并 Main，保留用户的
  未跟踪 `cc.md`。
- Agent 闭环：新增 `ThreadMutationAgentState`、类型化 ResumeInput/Update/Effect/Receipt、审批
  节点、决策节点和 EffectExecutor。Proposal 先持久化，再由节点主动
  `NodeResult::Suspend(Approval)`；拒绝产生零 Effect，批准只产生一个 Effect。
- 持久化恢复：新增 QQBot 独立 MySQL CheckpointStore，保存纯数据 AgentCheckpoint；进程重启后
  可由新 Runtime 实例加载并以 CAS 单次消费。Graph/State 版本、RunId、位置、预算、Receipt 和
  Suspend 数据均由 Agent Runtime 原协议校验。
- 权限与幂等：Resume 命令必须是 QQ 开放平台 `OwnerCommand`，且命令账号/Owner 与被管理
  NapCat 账号存在 active 本地绑定。普通 NapCat Observation、跨账号和不同决策默认拒绝；
  Effect ID 全局唯一，同一 Effect 重复执行返回 `changed=false`，`UnknownCommit` 禁止自动重放。
- 线程语义：Merge 第一条线程为 canonical，通过可撤销 alias 生效；Split 只允许真子集并生成
  新逻辑线程，通过事件 override 生效。`secretary_effective_thread_events` 已接入后续 Reply
  投影、语义提取和跨会话关联扫描；原始 `secretary_thread_events` 不物理搬移。新消息回复已被
  合并 alias 时，直接投影到 canonical thread。
- 数据库影响：新增迁移 `20260724_personal_secretary_thread_mutations.sql`，包含 Proposal、
  Checkpoint、Merge Alias、Split Override 和有效线程视图；仅位于 `apps/qqbot-server/database`，
  不修改数字人 `init.sql` 或数字人表。回滚顺序已写入迁移。
- 验证：personal-secretary 47 项常规测试通过；严格 Clippy 通过；全新隔离 MySQL 8.4 中 16 项
  串行集成回归通过，覆盖迁移幂等、未授权拒绝、持久化 Checkpoint 跨 Runtime 恢复、单次消费、
  Merge/Split、原始成员不变、Effect 幂等和合并后 Reply 进入 canonical thread。
- 外部系统影响：未连接 QQ 开放平台，未读取或配置 App ID/Secret/Owner OpenID，未调用
  NapCat 主动消息接口；测试只使用一次性本地 MySQL 容器，验收后容器与测试数据已删除。
- TODO 变化：THR-010、CMD-008 更新为后端闭环完成但控制面/反向操作仍待开发的 PARTIAL。
- 下一项：Owner 撤销/反向线程变更与已确认语义修订迁移，随后进入人物、项目和承诺记忆模型；
  进入 `CMD-001/CMD-002` 前继续执行凭据通知门禁。

## 后续记录模板

```text
## YYYY-MM-DD HH:mm（Asia/Shanghai）：切片名称

- 分支/提交：
- 完成范围：
- 未完成范围：
- 数据库影响：表、迁移、兼容与回滚
- 外部系统影响：NapCat、QQ 开放平台、模型、定时器
- 验证：测试命令、通过数量、实机场景
- 文档变化：
- 下一项与阻塞：
```
