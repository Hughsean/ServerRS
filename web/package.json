{
  "name": "serverrs-web",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "build": "pnpm --filter @serverrs/sdk build && pnpm --filter @serverrs/admin build",
    "dev": "pnpm --filter @serverrs/admin dev",
    "type-check": "pnpm -r type-check"
  }
}
