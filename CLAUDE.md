# Claude Code 项目记忆

本项目遵循通用工程规则：先理解上下文，再做最小必要修改，保持现有架构、命名、错误处理、测试和配置习惯。

进入任务后优先执行：

1. 明确本次修改属于哪个层次：接口层、应用层、领域层、基础设施层、配置层、测试层或工具脚本。
2. 修改前先阅读相关调用链，不要只看单个文件就直接改。
3. 优先复用已有类型、trait、错误模型、日志风格、配置读取方式和测试工具。
4. 不要进行无关重构、格式化大面积无关文件、替换技术栈或引入大型依赖。
5. 更新、添加、删除依赖时使用包管理器命令，例如 `cargo add/remove/update`、`pnpm add/remove/update`，不要直接手改依赖文件或锁文件。
6. 修改完成后说明改了什么、为什么这样改、如何验证。

常用验证命令按项目实际情况选择：

```bash
cargo fmt
cargo check
cargo test
```

更细的规则见 `.claude/rules/`。
