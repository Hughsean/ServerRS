# Application Layer

This directory contains use cases, orchestration, and inbound/outbound port traits. It may depend
on the domain layer and neutral core abstractions, but it must not depend on SeaORM, SQLx, MySQL,
QQ protocol adapters, or concrete worker implementations.
