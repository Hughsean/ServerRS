# Baseline v1 前历史迁移归档

这里保存 2026-08-03 Schema Baseline v1 压缩前的 33 个 QQBot 迁移，仅用于审计、结构来源核对和
识别旧数据库是否完整升级到压缩点。

这些文件不再参与全新数据库加载，也不得被生产或测试加载器逐个重放。需要从旧版本升级时，
必须先完整执行当时尚未应用的旧链并核对迁移记录，随后登记采用 Baseline v1；不能选择性执行
单个归档文件修补缺表。
