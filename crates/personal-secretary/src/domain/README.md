# Domain Layer

This directory contains protocol- and persistence-neutral entities, value objects, policies,
and invariants. Domain code must not depend on application services, database libraries, QQ
adapters, HTTP clients, or worker runtimes.
